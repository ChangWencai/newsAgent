---
phase: 02-safety
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - src/storage/database.py
  - src/scheduler/jobs.py
  - src/web/routes/api.py
  - src/web/templates/base.html
  - tests/test_database.py
autonomous: true
requirements:
  - SAFE-02
  - SAFE-03
user_setup: []

must_haves:
  truths:
    - "单日发布量不超过 5 篇（超过自动跳过而非报错）"
    - "发布间隔不少于 30 分钟（不足时自动跳过）"
    - "can_publish() 返回是否允许发布及原因"
    - "system_kv 表可存储系统级键值对"
    - "/health 端点返回 cookie_status 字段（valid/expired/missing）"
    - "Web UI 每 5 分钟轮询 /health，cookie 过期时显示通知 banner"
  artifacts:
    - path: "src/storage/database.py"
      provides: "频率控制方法 + cookie 状态方法 + system_kv 表"
      exports: ["can_publish", "get_today_publish_count", "get_last_publish_time", "set_cookie_status", "get_cookie_status"]
    - path: "src/scheduler/jobs.py"
      provides: "pipeline 入口添加 can_publish() 检查"
      contains: "can_publish"
    - path: "src/web/routes/api.py"
      provides: "/health 扩展 cookie_status"
      contains: "cookie_status"
    - path: "src/web/templates/base.html"
      provides: "cookie 过期通知 banner + 轮询 JS"
      contains: "checkCookieStatus"
  key_links:
    - from: "src/scheduler/jobs.py run_pipeline"
      to: "src/storage/database.py can_publish"
      via: "pipeline 入口频率检查"
      pattern: "db\\.can_publish"
    - from: "src/web/routes/api.py /health"
      to: "src/storage/database.py get_cookie_status"
      via: "读取 DB 缓存状态"
      pattern: "db\\.get_cookie_status"
    - from: "src/web/templates/base.html"
      to: "/health"
      via: "fetch 轮询"
      pattern: "fetch\\('/health'\\)"
---

<objective>
扩展 Database 类支持发布频率控制和 cookie 状态管理（SAFE-02, SAFE-03）

Purpose: 限制发布频率防止账号风控，cookie 过期时及时通知用户重新登录
Output: database.py 新增 can_publish/get_today_publish_count/get_last_publish_time/set_cookie_status/get_cookie_status 方法 + system_kv 表，jobs.py 集成频率检查，api.py 扩展 /health，base.html 添加 cookie 过期 banner
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-safety/02-CONTEXT.md
@.planning/phases/02-safety/02-RESEARCH.md
@src/storage/database.py
@src/scheduler/jobs.py
@src/web/routes/api.py
@src/web/templates/base.html
</context>

<interfaces>
Database 类现有接口:
```python
class Database:
    def _execute_write(self, sql, params=()): ...  # 写操作封装（自动 commit + Lock）
    def _execute_read(self, sql, params=()): ...   # 读操作封装
    def mark_published(self, article_id): ...      # 更新 status='published' + published_at
    def get_article(self, article_id) -> dict | None: ...
    # articles 表: status TEXT DEFAULT 'draft'（无 CHECK 约束，可扩展 'flagged'）
```

Pipeline 入口:
```python
def run_pipeline(db: Database):
    crawler = DouyinCrawler()
    writer = ArticleGenerator()
    pipeline = create_pipeline(crawler, writer, db)
    pipeline()
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 7: Database 频率控制 + cookie 状态方法</name>
  <files>src/storage/database.py, tests/test_database.py</files>
  <behavior>
    - _init_tables 新增 system_kv 表
    - can_publish(max_daily=5, min_interval_minutes=30) 返回 {allowed, reason, next_available}
    - 超过每日上限时 allowed=False + 原因
    - 间隔不足时 allowed=False + 等待时间
    - get_today_publish_count() 返回当日 published 文章数
    - get_last_publish_time() 返回最近发布时间或 None
    - set_cookie_status(status) 写入 system_kv
    - get_cookie_status() 返回 {status, updated_at}
  </behavior>
  <action>
    修改 src/storage/database.py：
    1. 在 _init_tables 的 executescript 中追加：
       CREATE TABLE IF NOT EXISTS system_kv (key TEXT PRIMARY KEY, value TEXT NOT NULL, updated_at TEXT NOT NULL);

    2. 新增方法：
       def can_publish(self, max_daily: int = 5, min_interval_minutes: int = 30) -> dict:
           count = self.get_today_publish_count()
           if count >= max_daily:
               return {"allowed": False, "reason": f"今日已发布 {count} 篇（上限 {max_daily}）", "next_available": "明天 00:00"}
           last_time = self.get_last_publish_time()
           if last_time:
               elapsed = (datetime.now() - last_time).total_seconds() / 60
               if elapsed < min_interval_minutes:
                   wait = min_interval_minutes - elapsed
                   return {"allowed": False, "reason": f"距上次发布仅 {elapsed:.0f} 分钟", "next_available": f"{wait:.0f} 分钟后"}
           return {"allowed": True, "reason": "", "next_available": ""}

       def get_today_publish_count(self) -> int:
           today = datetime.now().strftime("%Y-%m-%d")
           row = self._execute_read("SELECT COUNT(*) FROM articles WHERE status='published' AND published_at >= ?", (today,)).fetchone()
           return row[0] if row else 0

       def get_last_publish_time(self) -> datetime | None:
           row = self._execute_read("SELECT published_at FROM articles WHERE status='published' ORDER BY published_at DESC LIMIT 1").fetchone()
           if row and row[0]:
               return datetime.fromisoformat(row[0])
           return None

       def set_cookie_status(self, status: str):
           now = datetime.now().isoformat()
           self._execute_write("INSERT OR REPLACE INTO system_kv (key, value, updated_at) VALUES ('cookie_status', ?, ?)", (status, now))

       def get_cookie_status(self) -> dict:
           row = self._execute_read("SELECT value, updated_at FROM system_kv WHERE key = 'cookie_status'").fetchone()
           if row:
               return {"status": row[0], "updated_at": row[1]}
           return {"status": "missing", "updated_at": None}
  </action>
  <verify>
    <automated>python -m pytest tests/test_database.py -x -v -k "publish or cookie"</automated>
  </verify>
  <done>5 个新方法可调用，system_kv 表存在，频率控制逻辑正确</done>
</task>

<task type="auto" tdd="true">
  <name>Task 8: Pipeline 频率检查 + /health 扩展 + cookie banner</name>
  <files>src/scheduler/jobs.py, src/web/routes/api.py, src/web/templates/base.html</files>
  <behavior>
    - _run_pipeline_inner 开头调用 can_publish()，不通过则 log + return
    - /health 返回 {status, cookie_status, cookie_updated_at}
    - base.html 新增 JS 每 5 分钟轮询 /health
    - cookie_status == "expired" 时显示持久化 banner（missing 状态不触发，避免首次启动误报）
    - banner 有关闭按钮
  </behavior>
  <action>
    修改 src/scheduler/jobs.py：
    - _run_pipeline_inner 开头添加：
      check = db.can_publish(max_daily=5, min_interval_minutes=30)
      if not check["allowed"]:
          logger.info("发布跳过: %s, 下次: %s", check["reason"], check["next_available"])
          return

    修改 src/web/routes/api.py：
    - health 函数扩展为：
      def health():
          cookie = db.get_cookie_status()
          return {"status": "ok", "cookie_status": cookie["status"], "cookie_updated_at": cookie["updated_at"]}

    修改 src/web/templates/base.html：
    - 在 `</body>` 前添加 cookie 状态 banner 的 HTML 和 JS：
      <div id="cookie-banner" style="display:none; position:fixed; top:0; left:0; right:0; background:#c62828; color:#fff; padding:12px 24px; z-index:1000; text-align:center;">
        Cookie 已过期，请<a href="/login" style="color:#fff; text-decoration:underline;">重新登录</a>
        <button onclick="this.parentElement.style.display='none'" style="float:right; background:none; border:1px solid #fff; color:#fff; cursor:pointer; padding:2px 8px;">关闭</button>
      </div>
      <script>
        async function checkCookieStatus() {
          try {
            const res = await fetch('/health');
            const data = await res.json();
            if (data.cookie_status === 'expired') {
              document.getElementById('cookie-banner').style.display = 'block';
            }
          } catch(e) {}
        }
        setInterval(checkCookieStatus, 300000);
        checkCookieStatus();
      </script>
  </action>
  <verify>
    <automated>python -c "from src.scheduler.jobs import run_pipeline; from src.storage.database import Database; db = Database(':memory:'); run_pipeline(db); print('pipeline ok')"</automated>
  </verify>
  <done>pipeline 频率检查生效，/health 返回 cookie_status，Web UI 有 cookie 轮询逻辑</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scheduler→pipeline | APScheduler 触发流水线，内部频率控制决定是否执行 |
| health→client | /health 端点返回系统状态给前端 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Denial of Service | pipeline | mitigate | can_publish() 硬上限 5 篇/日 + 最小间隔 30 分钟，超限自动跳过 |
| T-02-08 | Information Disclosure | /health | mitigate | /health 仅返回 cookie 状态枚举值，不暴露具体 cookie 内容 |
</threat_model>

<verification>
- `python -m pytest tests/test_database.py -x -v` 全部通过
- `python -c "from src.storage.database import Database; db = Database(':memory:'); print(db.can_publish())"` 返回 `{'allowed': True, ...}`
- `/health` 端点返回 JSON 包含 `cookie_status` 字段
</verification>

<success_criteria>
1. can_publish() 正确判断每日上限和最小间隔
2. Pipeline 超限时 log 信息并跳过（非报错）
3. /health 返回 cookie_status 字段
4. Web UI 页面加载时检查 cookie 状态，5 分钟轮询
5. cookie_status == "expired" 时页面顶部显示红色通知 banner（missing 不触发）
</success_criteria>

<output>
完成后创建 `.planning/phases/02-safety/02-03-SUMMARY.md`
</output>
