---
phase: 02-safety
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/crawler/tophub.py
  - src/writer/generator.py
  - tests/test_retry.py
autonomous: true
requirements:
  - SAFE-08
user_setup: []

must_haves:
  truths:
    - "爬虫网络波动时自动重试，最多 3 次"
    - "AI API 调用失败时自动重试，最多 3 次"
    - "重试使用指数退避（1s, 2s, 4s...，最大 10s）"
    - "HTTP 429 和 5xx 触发重试，4xx（除 429）不重试"
    - "重试过程有日志记录（before_sleep_log）"
    - "最终失败时抛出原始异常（reraise=True）"
  artifacts:
    - path: "src/crawler/tophub.py"
      provides: "get_hot_list 添加 tenacity 重试装饰器"
      contains: "@crawl_retry"
    - path: "src/writer/generator.py"
      provides: "generate_article 添加 tenacity 重试装饰器"
      contains: "@api_retry"
    - path: "tests/test_retry.py"
      provides: "重试机制单元测试"
  key_links:
    - from: "src/crawler/tophub.py get_hot_list"
      to: "tenacity retry decorator"
      via: "@crawl_retry"
      pattern: "@crawl_retry"
    - from: "src/writer/generator.py generate_article"
      to: "tenacity retry decorator"
      via: "@api_retry"
      pattern: "@api_retry"
---

<objective>
为爬虫和 AI API 调用添加指数退避重试机制（SAFE-08）

Purpose: 网络波动和 API 限流不导致流水线中断，自动重试恢复
Output: tophub.py 和 generator.py 添加 tenacity 装饰器，test_retry.py 验证重试行为
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-safety/02-CONTEXT.md
@.planning/phases/02-safety/02-RESEARCH.md
@src/crawler/tophub.py
@src/writer/generator.py
</context>

<interfaces>
从 RESEARCH.md Pattern 6 提取的重试配置:

```python
from tenacity import retry, stop_after_attempt, wait_exponential, retry_if_exception_type, before_sleep_log

crawl_retry = retry(
    stop=stop_after_attempt(3),
    wait=wait_exponential(multiplier=1, min=1, max=10),
    retry=retry_if_exception_type((requests.RequestException,)),
    reraise=True,
    before_sleep=before_sleep_log(logger, logging.WARNING),
)

api_retry = retry(
    stop=stop_after_attempt(3),
    wait=wait_exponential(multiplier=1, min=1, max=10),
    retry=retry_if_exception_type((requests.RequestException, anthropic.APIConnectionError, anthropic.RateLimitError)),
    reraise=True,
    before_sleep=before_sleep_log(logger, logging.WARNING),
)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 4: 爬虫重试装饰器</name>
  <files>src/crawler/tophub.py</files>
  <behavior>
    - get_hot_list 被 @crawl_retry 装饰
    - requests.RequestException 触发重试
    - 最多重试 3 次，指数退避 1-10 秒
    - 重试前记录 WARNING 日志
    - 最终失败抛出原始 RequestException
  </behavior>
  <action>
    修改 src/crawler/tophub.py：
    - 添加 tenacity 导入: `from tenacity import retry, stop_after_attempt, wait_exponential, retry_if_exception_type, before_sleep_log`
    - 模块级定义 `crawl_retry = retry(stop=stop_after_attempt(3), wait=wait_exponential(multiplier=1, min=1, max=10), retry=retry_if_exception_type((requests.RequestException,)), reraise=True, before_sleep=before_sleep_log(logger, logging.WARNING))`
    - DouyinCrawler.get_hot_list 方法上方添加 `@crawl_retry`
    - 修改异常处理：让 RequestException 传播给装饰器，仅保留非网络异常的兜底
  </action>
  <verify>
    <automated>python -c "from src.crawler.tophub import DouyinCrawler; c = DouyinCrawler(); print('decorator ok')"</automated>
  </verify>
  <done>get_hot_list 带有 @crawl_retry 装饰器，导入无错误</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: AI API 重试装饰器</name>
  <files>src/writer/generator.py</files>
  <behavior>
    - generate_article 被 @api_retry 装饰
    - requests.RequestException, anthropic.APIConnectionError, anthropic.RateLimitError 触发重试
    - 最多重试 3 次，指数退避 1-10 秒
    - 最终失败抛出原始异常
  </behavior>
  <action>
    修改 src/writer/generator.py：
    - 添加 tenacity 导入
    - 添加 anthropic 异常导入（已有 `import anthropic`，使用 `anthropic.APIConnectionError`, `anthropic.RateLimitError`）
    - 模块级定义 `api_retry = retry(stop=stop_after_attempt(3), wait=wait_exponential(multiplier=1, min=1, max=10), retry=retry_if_exception_type((requests.RequestException, anthropic.APIConnectionError, anthropic.RateLimitError)), reraise=True, before_sleep=before_sleep_log(logger, logging.WARNING))`
    - ArticleGenerator.generate_article 方法上方添加 `@api_retry`
  </action>
  <verify>
    <automated>python -c "from src.writer.generator import ArticleGenerator; print('decorator ok')"</automated>
  </verify>
  <done>generate_article 带有 @api_retry 装饰器，导入无错误</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: 重试机制测试</name>
  <files>tests/test_retry.py</files>
  <behavior>
    - Mock 网络异常后成功：验证重试后返回结果
    - Mock 持续异常：验证最终抛出异常且重试次数正确
    - crawl_retry 和 api_retry 装饰器可独立使用
  </behavior>
  <action>
    创建 tests/test_retry.py：
    - test_crawl_retry_on_network_error: mock requests.get 前 2 次抛 ConnectionError，第 3 次返回正常 -> 成功
    - test_crawl_retry_exhausted: mock 始终抛 ConnectionError -> 最终抛出 ConnectionError
    - test_api_retry_on_rate_limit: mock 前 1 次抛 RateLimitError，第 2 次返回正常 -> 成功
    - test_crawl_retry_does_not_retry_4xx: mock 返回 HTTP 404 -> 不重试，直接 raise_for_status
  </action>
  <verify>
    <automated>python -m pytest tests/test_retry.py -x -v</automated>
  </verify>
  <done>4 个重试测试全部通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crawler→external API | 爬虫请求外部 API 可能被限流或断连 |
| generator→MiniMax API | AI API 调用可能超时或被限流 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Denial of Service | tophub.py | mitigate | tenacity 指数退避重试，stop=3 防止无限循环 |
| T-02-06 | Denial of Service | generator.py | mitigate | tenacity 指数退避 + SDK 内置重试，保守 stop=3 |
</threat_model>

<verification>
- `python -c "from src.crawler.tophub import DouyinCrawler"` 导入无异常
- `python -c "from src.writer.generator import ArticleGenerator"` 导入无异常
- `python -m pytest tests/test_retry.py -x -v` 4 个测试全部通过
</verification>

<success_criteria>
1. tophub.py 的 get_hot_list 带有 @crawl_retry 装饰器
2. generator.py 的 generate_article 带有 @api_retry 装饰器
3. 网络异常时自动重试最多 3 次，指数退避
4. test_retry.py 4 个测试全部通过
</success_criteria>

<output>
完成后创建 `.planning/phases/02-safety/02-02-SUMMARY.md`
</output>
