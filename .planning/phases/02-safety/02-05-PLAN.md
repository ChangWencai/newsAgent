---
phase: 02-safety
plan: 05
type: execute
wave: 4
depends_on: []
files_modified:
  - src/publisher/toutiao_publisher.py
  - src/pipeline/jobs.py
  - tests/test_publisher.py
autonomous: true
requirements:
  - SAFE-01
  - SAFE-07
user_setup: []

must_haves:
  truths:
    - "playwright-stealth 集成到浏览器上下文创建流程"
    - "发布页面的标题和正文输入使用语义定位器"
    - "发布按钮使用语义定位器或 data-testid 备选"
    - "选择器超时后自动截图保存并记录日志"
    - "随机延迟保留作为反检测补充措施"
  artifacts:
    - path: "src/publisher/toutiao_publisher.py"
      provides: "playwright-stealth 集成 + 语义定位器迁移 + 超时截图"
      contains: "stealth"
    - path: "tests/test_publisher.py"
      provides: "发布模块单元测试"
  key_links:
    - from: "ToutiaoPublisher.publish"
      to: "playwright_stealth.Stealth"
      via: "Stealth().use_async(async_playwright())"
      pattern: "Stealth"
    - from: "ToutiaoPublisher._do_publish"
      to: "page.get_by_role / page.get_by_placeholder"
      via: "语义定位器替换 CSS class"
      pattern: "get_by_"
    - from: "selector timeout"
      to: "page.screenshot + logger.error"
      via: "异常捕获后截图保存"
      pattern: "screenshot"
    - from: "ToutiaoPublisher._check_login"
      to: "db.set_cookie_status"
      via: "publisher 检测结果写入 DB"
      pattern: "set_cookie_status"
    - from: "src/pipeline/jobs.py _run_pipeline_inner"
      to: "ToutiaoPublisher.__init__"
      via: "pipeline 传递 db 给 publisher"
      pattern: "publisher.*=.*ToutiaoPublisher"
---

<objective>
加固 Playwright 发布模块：集成反检测和语义定位器（SAFE-01, SAFE-07）

Purpose: 降低头条平台自动化检测风险，提高选择器对 UI 变更的抵抗力
Output: toutiao_publisher.py 集成 playwright-stealth + 语义定位器迁移 + 超时截图诊断，test_publisher.py 测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-safety/02-CONTEXT.md
@.planning/phases/02-safety/02-RESEARCH.md
@src/publisher/toutiao_publisher.py
</context>

<interfaces>
toutiao_publisher.py 现有结构:
```python
class ToutiaoPublisher:
    async def publish(self, title, content, headless=False): ...    # 主入口
    async def _create_context(self, browser): ...                   # 创建上下文 + 加载 cookie
    async def _check_login(self, page): ...                         # 检查登录状态
    async def _wait_login(self, page, context): ...                 # 等待手动登录
    async def _do_publish(self, page, title, content): ...          # 执行发布
    async def _select_cover(self, page): ...                        # 选择封面

# 现有 CSS 选择器（需迁移）：
#   "textarea"                         → 标题输入
#   ".ProseMirror"                     → 正文编辑器
#   "button.publish-btn-last"          → 发布按钮
#   ".article-cover-images-wrap ..."   → 封面选择（嵌套 CSS class）
#   "text=我的素材"                    → 素材 tab
#   ".byte-drawer .img-span"           → 图片选择
#   "div.byte-drawer .footer button.byte-btn-primary" → 确认按钮
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 11: playwright-stealth 集成</name>
  <files>src/publisher/toutiao_publisher.py</files>
  <behavior>
    - publish 方法使用 Stealth().use_async(async_playwright()) 替换 async_playwright()
    - 浏览器上下文自动注入反检测脚本
    - 随机延迟保留（random.uniform）
    - 导入无报错
  </behavior>
  <action>
    修改 src/publisher/toutiao_publisher.py：
    - 顶部添加: `from playwright_stealth import Stealth`
    - publish 方法中替换:
      原: `async with async_playwright() as p:`
      改: `async with Stealth().use_async(async_playwright()) as p:`
    - 保留所有 random.uniform 延迟不变
  </action>
  <verify>
    <automated>python -c "from src.publisher.toutiao_publisher import ToutiaoPublisher; print('stealth import ok')"</automated>
  </verify>
  <done>Stealth 集成到 publish 方法，导入和实例化无报错</done>
</task>

<task type="auto" tdd="true">
  <name>Task 12: 语义定位器迁移 + 超时截图</name>
  <files>src/publisher/toutiao_publisher.py, tests/test_publisher.py</files>
  <behavior>
    - 标题输入: page.get_by_role("textbox") 替换 page.fill("textarea", ...)
    - 正文输入: page.locator(".ProseMirror") 保留（头条编辑器无语义属性，兜底）
    - 发布按钮: page.get_by_role("button", name="发布") 替换 wait_for_selector("button.publish-btn-last")
    - 选择器超时时截图保存到 data/screenshots/ + 日志记录
    - 封面选择部分保留 CSS class（头条特有组件，无语义属性）
  </behavior>
  <action>
    修改 src/publisher/toutiao_publisher.py：

    1. publish 方法添加截图目录初始化:
       screenshot_dir = os.path.join(os.path.dirname(...), "data", "screenshots")
       os.makedirs(screenshot_dir, exist_ok=True)

    2. _do_publish 方法选择器迁移:
       - await page.wait_for_selector("textarea", timeout=15000)
         → await page.get_by_role("textbox").first.wait_for(timeout=15000)
       - await page.fill("textarea", title)
         → await page.get_by_role("textbox").first.fill(title)
       - await page.fill(".ProseMirror", content)
         → 保留 page.locator(".ProseMirror")，头条编辑器无语义属性，使用 locator 作为兜底
       - publish_btn = await page.wait_for_selector("button.publish-btn-last", ...)
         → publish_btn = page.get_by_role("button", name="发布")
           await publish_btn.wait_for(timeout=10000)

    3. _do_publish 添加超时截图:
       在 try/except 中包裹关键选择器操作，超时时:
       - screenshot_path = os.path.join(screenshot_dir, f"timeout_{int(time.time())}.png")
       - await page.screenshot(path=screenshot_path)
       - logger.error("选择器超时，截图已保存: %s", screenshot_path)

    4. _select_cover 保留 CSS class（头条特有组件，D-18 允许最后手段保留）

    创建 tests/test_publisher.py：
    - test_stealth_import: playwright_stealth 可导入
    - test_publisher_instantiation: ToutiaoPublisher() 可实例化
    - test_screenshot_dir_created: 实例化后 data/screenshots 目录存在
  </action>
  <verify>
    <automated>python -m pytest tests/test_publisher.py -x -v</automated>
  </verify>
  <done>关键选择器使用语义定位器，超时时截图保存，3 个测试通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 13: Publisher cookie 状态写入 DB</name>
  <files>src/publisher/toutiao_publisher.py, src/pipeline/jobs.py, tests/test_publisher.py</files>
  <behavior>
    - ToutiaoPublisher 构造函数接受可选 db 参数
    - _check_login 返回后调用 db.set_cookie_status("valid"/"expired")
    - _wait_login 成功后调用 db.set_cookie_status("valid")
    - pipeline 创建 publisher 时传入 db
    - 无 db 参数时 set_cookie_status 调用被跳过（向后兼容）
  </behavior>
  <action>
    修改 src/publisher/toutiao_publisher.py：
    1. __init__ 添加 db=None 参数，self.db = db
    2. _check_login 成功分支添加: if self.db: self.db.set_cookie_status("valid")
    3. _check_login 失败分支添加: if self.db: self.db.set_cookie_status("expired")
    4. _wait_login 成功后添加: if self.db: self.db.set_cookie_status("valid")

    修改 src/pipeline/jobs.py：
    1. create_pipeline 中创建 publisher 时传入 db: ToutiaoPublisher(db=db)

    扩展 tests/test_publisher.py：
    - test_cookie_status_set_on_login: mock db，验证 _check_login 调用 set_cookie_status
    - test_cookie_status_skipped_without_db: 无 db 参数时不报错
    - test_cookie_status_set_on_wait_login: mock db，验证 _wait_login 成功后更新状态
  </action>
  <verify>
    <automated>python -m pytest tests/test_publisher.py -x -v</automated>
  </verify>
  <done>publisher 每次登录检测后更新 cookie 状态到 DB，pipeline 传递 db 给 publisher</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| publisher→toutiao | Playwright 浏览器访问头条平台，需绕过反爬检测 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Denial of Service | toutiao_publisher.py | mitigate | playwright-stealth 绕过基础 bot 检测 + 随机延迟模拟人工操作 + 频率控制 |
| T-02-12 | Tampering | selector timeout | mitigate | 超时截图 + 详细日志，便于定位头条 UI 变更 |
</threat_model>

<verification>
- `python -c "from playwright_stealth import Stealth; print('stealth ok')"` 导入无报错
- `python -m pytest tests/test_publisher.py -x -v` 3 个测试通过
- toutiao_publisher.py 中关键选择器不再使用纯 CSS class
</verification>

<success_criteria>
1. playwright-stealth 集成到 publish 方法
2. 标题和正文输入使用语义定位器
3. 发布按钮使用 get_by_role("button", name="发布")
4. 封面选择保留 CSS class（头条特有组件，语义定位器不可用）
5. 选择器超时时截图保存到 data/screenshots/ 并记录错误日志
6. publisher 登录检测后调用 db.set_cookie_status() 更新 cookie 状态
7. pipeline 创建 publisher 时传入 db
8. test_publisher.py 全部测试通过
</success_criteria>

<output>
完成后创建 `.planning/phases/02-safety/02-05-SUMMARY.md`
</output>
