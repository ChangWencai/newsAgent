---
phase: 03-multi-source
plan: "05"
type: execute
wave: 3
depends_on:
  - "02"
  - "03"
  - "04"
files_modified:
  - src/scheduler/jobs.py
  - main.py
  - tests/test_pipeline.py
  - tests/test_dedup.py
autonomous: true
requirements:
  - MULTI-08
  - MULTI-09

must_haves:
  truths:
    - "dedup_topics 函数按标题相似度去重，保留每组热度最高的条目"
    - "create_pipeline(db) 签名变更为单参数，不再接收 crawler 和 writer"
    - "pipeline 遍历 get_registered_crawlers() 自动调度多源抓取"
    - "单个爬虫失败不影响其他爬虫"
    - "main.py 导入爬虫模块触发 register_crawler"
    - "run_pipeline(db) 正常工作"
    - "已有 pipeline 测试更新后全部通过"
  artifacts:
    - path: "src/scheduler/jobs.py"
      provides: "多源 pipeline + dedup_topics 函数"
      contains: "def dedup_topics"
      contains2: "get_registered_crawlers"
    - path: "main.py"
      provides: "爬虫模块导入触发注册"
    - path: "tests/test_dedup.py"
      provides: "去重逻辑单元测试"
    - path: "tests/test_pipeline.py"
      provides: "更新后的 pipeline 集成测试"
  key_links:
    - from: "src/scheduler/jobs.py"
      to: "src/crawler/__init__.py"
      via: "import get_registered_crawlers"
      pattern: "from src.crawler import get_registered_crawlers"
    - from: "main.py"
      to: "src/crawler/*.py"
      via: "import 触发 register_crawler"
      pattern: "import src.crawler"
    - from: "src/scheduler/jobs.py"
      to: "difflib"
      via: "SequenceMatcher 去重"
      pattern: "difflib.SequenceMatcher"
---

<objective>
实现跨平台去重算法和多源 pipeline 调度，将现有 pipeline 从单一爬虫调用改造为遍历所有注册爬虫。

Purpose: Phase 3 核心整合任务，将前 4 个计划的产出（类型、注册、适配器）串联为完整的多源工作流。同时更新 main.py 确保爬虫模块被导入时自动注册。
Output: 改造后的 jobs.py (dedup_topics + create_pipeline 签名变更), main.py (爬虫导入), test_dedup.py, 更新后的 test_pipeline.py。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-multi-source/03-CONTEXT.md
@.planning/phases/03-multi-source/03-RESEARCH.md
@.planning/phases/03-multi-source/03-PATTERNS.md
@src/scheduler/jobs.py
@main.py
@tests/test_pipeline.py

<interfaces>
From src/crawler/__init__.py (Plan 01 创建):
```python
def get_registered_crawlers() -> list[CrawlerProtocol]: ...
```

From src/crawler/models.py (Plan 01 创建):
```python
@dataclass(frozen=True)
class HotTopic:
    title: str
    url: str
    source: str
    hot_value: str = ""
    category: str = ""
    fetched_at: str = ""
```

From src/storage/database.py (Plan 01 修改):
```python
def insert_topic(self, title, url="", hot_value="", category="", source="") -> int
def topic_exists(self, title) -> bool
```

现有 jobs.py 关键结构（需要改造的部分）:
- 第14行: def create_pipeline(crawler, writer, db) -> 改为 create_pipeline(db)
- 第28行: def _run_pipeline_inner(crawler, writer, db) -> 删除，逻辑合并
- 第39行: topics = crawler.get_hot_list() -> 改为遍历多爬虫
- 第48行: topic["title"] 字典访问 -> 改为 topic.title 属性访问
- 第105行: def run_pipeline(db) -> 内部不再创建 DouyinCrawler/ArticleGenerator
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 dedup_topics 去重函数 + 创建去重测试</name>
  <read_first>
    src/crawler/models.py (HotTopic dataclass)
    src/scheduler/jobs.py (当前导入和结构)
  </read_first>
  <files>src/scheduler/jobs.py, tests/test_dedup.py</files>
  <behavior>
    - Test 1: 相似标题(>60%)去重，保留 hot_value 更高的
    - Test 2: 不同标题不去重，全部保留
    - Test 3: 空列表返回空列表
    - Test 4: 跨平台同事件去重（如 "人工智能大会召开" vs "人工智能大会今日召开" vs "AI大会召开"）
    - Test 5: 短数字 hot_value 正确排序（"9" vs "100" 应保留 "100"，验证数值排序而非字典序）
  </behavior>
  <action>
    1. 在 src/scheduler/jobs.py 顶部导入区域添加：
       ```python
       import difflib
       from src.crawler.protocol import HotTopic
       ```
    2. 在 create_pipeline 函数之前添加 dedup_topics 函数（per D-15, D-16, RESEARCH.md Pattern 3）：
       ```python
       def dedup_topics(topics: list[HotTopic], threshold: float = 0.6) -> list[HotTopic]:
           """按标题相似度分组，每组保留 hot_value 最高的条目"""
           if not topics:
               return []
           groups: list[list[HotTopic]] = []
           for topic in sorted(topics, key=lambda t: int(t.hot_value or 0), reverse=True):
               matched = False
               for group in groups:
                   if difflib.SequenceMatcher(None, topic.title, group[0].title).ratio() >= threshold:
                       group.append(topic)
                       matched = True
                       break
               if not matched:
                   groups.append([topic])
           return [g[0] for g in groups]
       ```
    3. 创建 tests/test_dedup.py，4 个测试用例：
       - test_similar_titles_dedup: 两个相似标题保留 hot_value 更高的
       - test_different_titles_no_dedup: 两个不同标题全部保留
       - test_empty_list: 空列表返回空
       - test_cross_platform_same_event: 3 个相似标题保留最高热度的 1 个
  </action>
  <verify>
    <automated>cd /Users/wencai/github/newsAgent && python -m pytest tests/test_dedup.py -x -v</automated>
  </verify>
  <done>
    src/scheduler/jobs.py 包含 dedup_topics(topics, threshold=0.6) 函数，使用 difflib.SequenceMatcher 按标题相似度去重。tests/test_dedup.py 4 个测试全部通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: 改造 create_pipeline 为多源调度 + 更新 main.py + 更新测试</name>
  <read_first>
    src/scheduler/jobs.py (当前 create_pipeline 三参签名，第14-20行; _run_pipeline_inner 第28-102行; run_pipeline 第105-110行)
    main.py (当前 main 函数第54-98行, create_app 函数第26-51行)
    tests/test_pipeline.py (当前使用旧签名的 7 个测试)
    src/crawler/__init__.py (get_registered_crawlers)
  </read_first>
  <files>src/scheduler/jobs.py, main.py, tests/test_pipeline.py</files>
  <action>
    1. 修改 src/scheduler/jobs.py 顶部导入（per D-18）：
       - 删除: `from src.crawler.tophub import DouyinCrawler`
       - 删除: `from src.writer.generator import ArticleGenerator`（移到 pipeline 内部）
       - 添加: `from src.crawler import get_registered_crawlers`
       - 保留其余导入不变
    2. 删除 create_pipeline 旧签名（第14-20行）和 _run_pipeline_inner 函数（第28-102行）。
    3. 用新的多源 pipeline 替换，create_pipeline 签名改为 `(db)` 单参数（per D-18, D-19, RESEARCH.md Pitfall 1）：
       - 内部使用 get_registered_crawlers() 遍历所有注册爬虫
       - 每个爬虫独立 try/except（per D-19），失败时 logger.error 记录
       - 收集所有 HotTopic 后调用 dedup_topics 组内去重
       - 然后执行频率控制检查（保留原有 can_publish 逻辑）
       - 然后执行 DB 历史去重（保留原有 topic_exists 逻辑）
       - insert_topic 调用添加 source=topic.source 参数
       - ArticleGenerator 在 pipeline 内部创建（不再通过构造函数注入）
       - 保留敏感词检查和异常处理逻辑
    4. 修改 run_pipeline 函数（第105-110行）：
       ```python
       def run_pipeline(db: Database):
           """兼容入口：通过注册列表获取爬虫"""
           pipeline = create_pipeline(db)
           pipeline()
       ```
    5. 修改 main.py，在 main() 函数 argparse 之后添加爬虫模块导入（per D-08, RESEARCH.md Pitfall 1）：
       ```python
       # 导入爬虫模块触发自动注册
       import src.crawler.tophub  # noqa: F401
       import src.crawler.weibo   # noqa: F401
       import src.crawler.zhihu   # noqa: F401
       import src.crawler.baidu   # noqa: F401
       ```
       同样在 create_app() 函数中添加相同导入（确保 Web 模式也能注册爬虫）。
    6. 更新 tests/test_pipeline.py，将所有测试从旧签名迁移到新签名：
       - 所有 create_pipeline(mock_crawler, mock_writer, db) 改为 create_pipeline(db)
       - 使用 @patch("src.scheduler.jobs.get_registered_crawlers") 注入 mock 爬虫
       - 使用 @patch("src.scheduler.jobs.ArticleGenerator") 注入 mock writer
       - mock 爬虫返回 HotTopic 对象（而非 dict），需要设置 mock_crawler.__class__.__name__
       - 保留原有测试逻辑（no_topics, dedup, full_flow, writer_failure, daily_limit, sensitive, normal_draft）
  </action>
  <verify>
    <automated>cd /Users/wencai/github/newsAgent && python -m pytest tests/test_pipeline.py tests/test_dedup.py -x -v</automated>
  </verify>
  <done>
    create_pipeline(db) 签名改为单参数，内部使用 get_registered_crawlers() 遍历所有爬虫。dedup_topics 去重在 DB 历史去重之前执行。main.py 导入 4 个爬虫模块触发自动注册。test_pipeline.py 全部测试更新为新签名并通过。run_pipeline(db) 正常工作。
  </done>
</task>

</tasks>

<verification>
```bash
# 全部 Phase 3 新测试通过
python -m pytest tests/test_models.py tests/test_registry.py tests/test_adapters.py tests/test_dedup.py tests/test_pipeline.py -x -v

# 已有测试无回归
python -m pytest tests/ -x -v

# 爬虫注册正常
python -c "
import src.crawler.tophub
import src.crawler.weibo
import src.crawler.zhihu
import src.crawler.baidu
from src.crawler import get_registered_crawlers
crawlers = get_registered_crawlers()
names = [type(c).__name__ for c in crawlers]
print(f'已注册爬虫: {names}')
assert len(names) == 4, f'期望 4 个爬虫，实际 {len(names)} 个'
"

# Pipeline 签名正确
python -c "
from src.scheduler.jobs import create_pipeline
import inspect
sig = inspect.signature(create_pipeline)
params = list(sig.parameters.keys())
assert params == ['db'], f'期望 [db]，实际 {params}'
print('create_pipeline 签名正确: (db)')
"
```
</verification>

<success_criteria>
- src/scheduler/jobs.py 包含 dedup_topics(topics, threshold=0.6) 函数
- create_pipeline(db) 签名变更为单参数（不再接收 crawler, writer）
- create_pipeline 内部使用 get_registered_crawlers() 遍历爬虫
- 单个爬虫失败时 try/except 捕获并记录日志，不影响其他爬虫
- pipeline 内部创建 ArticleGenerator 实例用于文章生成
- insert_topic 调用包含 source=topic.source 参数
- main.py 的 main() 和 create_app() 中导入 4 个爬虫模块
- run_pipeline(db) 正常工作
- tests/test_dedup.py 4 个测试通过
- tests/test_pipeline.py 全部测试更新为新签名并通过
- 88 个已有测试无回归
- pytest tests/ 全部通过
</success_criteria>

<output>
完成后创建 .planning/phases/03-multi-source/03-05-SUMMARY.md
</output>
