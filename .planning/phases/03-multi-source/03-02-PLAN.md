---
phase: 03-multi-source
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/crawler/tophub.py
autonomous: true
requirements:
  - MULTI-08

must_haves:
  truths:
    - "DouyinCrawler.get_hot_list() 返回 list[HotTopic] 而非 list[dict]"
    - "tophub.py 末尾调用 register_crawler(DouyinCrawler())"
    - "tenacity 重试装饰器保留不变"
    - "已有 retry 测试继续通过"
  artifacts:
    - path: "src/crawler/tophub.py"
      provides: "升级后的 DouyinCrawler"
      contains: "from src.crawler.protocol import HotTopic"
      contains2: "register_crawler(DouyinCrawler())"
  key_links:
    - from: "src/crawler/tophub.py"
      to: "src/crawler/protocol.py"
      via: "import HotTopic"
      pattern: "from src.crawler.protocol import HotTopic"
    - from: "src/crawler/tophub.py"
      to: "src/crawler/__init__.py"
      via: "import register_crawler"
      pattern: "from src.crawler import register_crawler"
---

<objective>
将现有 DouyinCrawler 迁移到新协议：返回值从 list[dict] 升级为 list[HotTopic]，模块末尾自动注册到 CRAWLERS 注册中心。

Purpose: 确保现有抖音爬虫兼容新的多源架构，为 pipeline 多源遍历做准备。保留 tenacity 重试逻辑不变。
Output: 修改后的 tophub.py，DouyinCrawler 实现 CrawlerProtocol 且自动注册。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-multi-source/03-CONTEXT.md
@.planning/phases/03-multi-source/03-RESEARCH.md
@.planning/phases/03-multi-source/03-PATTERNS.md
@src/crawler/tophub.py

<interfaces>
From src/crawler/protocol.py (Plan 01 创建):
```python
class CrawlerProtocol(Protocol):
    def get_hot_list(self) -> list[HotTopic]: ...
```

From src/crawler/__init__.py (Plan 01 创建):
```python
def register_crawler(crawler: CrawlerProtocol) -> None: ...
```

From src/crawler/models.py (Plan 01 创建):
```python
@dataclass(frozen=True)
class HotTopic:
    title: str
    url: str
    source: str
    hot_value: str = ""
    category: str = ""
    fetched_at: str = ""
```

现有 tophub.py 结构（保留不变的部分）:
- 第11-32行: _is_retryable() + crawl_retry 装饰器
- 第34-38行: DOUYIN_HOT_URL, HEADERS 常量
- 第41行: class DouyinCrawler
- 第42-43行: @crawl_retry + def get_hot_list(self)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 迁移 DouyinCrawler 到 HotTopic 返回值 + 自动注册</name>
  <read_first>
    src/crawler/tophub.py (当前实现: 返回 list[dict]，第53-62行)
    src/crawler/models.py (HotTopic dataclass 字段)
    src/crawler/__init__.py (register_crawler 函数)
  </read_first>
  <files>src/crawler/tophub.py</files>
  <action>
    1. 在 tophub.py 顶部添加两个新导入（per D-08）：
       ```python
       from src.crawler.protocol import HotTopic
       from src.crawler import register_crawler
       ```
    2. 修改 get_hot_list 方法（第43-62行），返回值从 list[dict] 改为 list[HotTopic]：
       - 保留 @crawl_retry 装饰器不变
       - 保留 requests.get(DOUYIN_HOT_URL, headers=HEADERS, timeout=15) 不变
       - 保留 word_list = data.get("data", {}).get("word_list", []) 不变
       - 将 results.append({...}) 改为 results.append(HotTopic(...))，字段映射：
         - title=item.get("word", "")
         - url=f"https://www.douyin.com/search/{item.get('word', '')}"
         - source="douyin"
         - hot_value=str(item.get("hot_value", 0))
         - category="抖音热搜"
    3. 在文件末尾添加自动注册调用：
       ```python
       register_crawler(DouyinCrawler())
       ```
  </action>
  <verify>
    <automated>cd /Users/wencai/github/newsAgent && python -c "from src.crawler.tophub import DouyinCrawler; from src.crawler import get_registered_crawlers; crawlers = get_registered_crawlers(); print(f'Registered: {len(crawlers)} crawlers'); assert any(type(c).__name__ == 'DouyinCrawler' for c in crawlers), 'DouyinCrawler not registered'"</automated>
  </verify>
  <done>
    tophub.py 导入 HotTopic 和 register_crawler。DouyinCrawler.get_hot_list() 返回 list[HotTopic]。文件末尾调用 register_crawler(DouyinCrawler())。tenacity 重试装饰器保留不变。导入后 DouyinCrawler 自动出现在 get_registered_crawlers() 列表中。
  </done>
</task>

</tasks>

<verification>
```bash
# DouyinCrawler 自动注册
python -c "from src.crawler.tophub import DouyinCrawler; from src.crawler import get_registered_crawlers; assert any(type(c).__name__ == 'DouyinCrawler' for c in get_registered_crawlers()); print('DouyinCrawler registered OK')"

# 返回值类型正确
python -c "from src.crawler.tophub import DouyinCrawler; c = DouyinCrawler(); from src.crawler.models import HotTopic; print(f'DouyinCrawler implements CrawlerProtocol')"

# 已有 retry 测试不受影响
python -m pytest tests/test_retry.py -x -v
```
</verification>

<success_criteria>
- src/crawler/tophub.py 导入 from src.crawler.protocol import HotTopic
- src/crawler/tophub.py 导入 from src.crawler import register_crawler
- DouyinCrawler.get_hot_list() 方法返回 list[HotTopic]（for 循环中使用 HotTopic(...) 构造）
- tophub.py 末尾有 register_crawler(DouyinCrawler()) 调用
- crawl_retry 装饰器、_is_retryable 函数、DOUYIN_HOT_URL、HEADERS 保留不变
- 导入 src.crawler.tophub 后 get_registered_crawlers() 包含 DouyinCrawler 实例
- tests/test_retry.py 全部通过（无回归）
</success_criteria>

<output>
完成后创建 .planning/phases/03-multi-source/03-02-SUMMARY.md
</output>
