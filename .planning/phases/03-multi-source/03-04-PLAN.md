---
phase: 03-multi-source
plan: "04"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/crawler/weibo.py
  - src/crawler/zhihu.py
  - src/crawler/baidu.py
  - tests/test_adapters.py
autonomous: true
requirements:
  - MULTI-05
  - MULTI-06
  - MULTI-07

must_haves:
  truths:
    - "WeiboCrawler.get_hot_list() 返回 list[HotTopic]，source='weibo'"
    - "ZhihuCrawler.get_hot_list() 返回 list[HotTopic]，source='zhihu'"
    - "BaiduCrawler.get_hot_list() 返回 list[HotTopic]，source='baidu'"
    - "每个适配器调用 fetch_dailyhot 获取数据"
    - "每个适配器模块末尾调用 register_crawler 自动注册"
  artifacts:
    - path: "src/crawler/weibo.py"
      provides: "微博热搜适配器"
      contains: "source=\"weibo\""
    - path: "src/crawler/zhihu.py"
      provides: "知乎热榜适配器"
      contains: "source=\"zhihu\""
    - path: "src/crawler/baidu.py"
      provides: "百度热搜适配器"
      contains: "source=\"baidu\""
    - path: "tests/test_adapters.py"
      provides: "适配器 mock 测试"
  key_links:
    - from: "src/crawler/weibo.py"
      to: "src/crawler/dailyhot.py"
      via: "import fetch_dailyhot"
      pattern: "from src.crawler.dailyhot import fetch_dailyhot"
---

<objective>
创建微博、知乎、百度三个平台的热搜适配器，每个适配器通过 DailyHotApi 获取数据并返回统一 HotTopic 格式。

Purpose: 扩展热点来源到 3 个新平台，每个适配器遵循相同模式（fetch_dailyhot + 字段映射 + register_crawler），确保 pipeline 可自动发现和调度。
Output: weibo.py, zhihu.py, baidu.py 三个适配器模块 + test_adapters.py 单元测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-multi-source/03-CONTEXT.md
@.planning/phases/03-multi-source/03-RESEARCH.md
@.planning/phases/03-multi-source/03-PATTERNS.md
@src/crawler/dailyhot.py

<interfaces>
From src/crawler/dailyhot.py (Plan 03 创建):
```python
def fetch_dailyhot(platform: str) -> list[dict[str, Any]]:
```

From src/crawler/models.py (Plan 01 创建):
```python
@dataclass(frozen=True)
class HotTopic:
    title: str
    url: str
    source: str
    hot_value: str = ""
    category: str = ""
    fetched_at: str = ""
```

From src/crawler/__init__.py (Plan 01 创建):
```python
def register_crawler(crawler: CrawlerProtocol) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建微博热搜适配器 WeiboCrawler</name>
  <read_first>
    src/crawler/dailyhot.py (fetch_dailyhot 函数签名)
    src/crawler/models.py (HotTopic 字段)
    src/crawler/weibo.py (不存在，将新建)
  </read_first>
  <files>src/crawler/weibo.py, tests/test_adapters.py</files>
  <behavior>
    - Test 1: WeiboCrawler.get_hot_list() 返回 list[HotTopic]，每个 source="weibo"
    - Test 2: fetch_dailyhot 返回空列表时 get_hot_list 返回空列表
    - Test 3: HotTopic 字段正确映射（title, url, hot_value, category）
  </behavior>
  <action>
    1. 创建 src/crawler/weibo.py（per D-11, RESEARCH.md Pattern 2）：
       ```python
       """微博热搜适配器 - 通过 DailyHotApi 获取微博热榜"""
       import logging
       from src.crawler.protocol import HotTopic
       from src.crawler.dailyhot import fetch_dailyhot
       from src.crawler import register_crawler

       logger = logging.getLogger(__name__)


       class WeiboCrawler:
           def get_hot_list(self) -> list[HotTopic]:
               raw_items = fetch_dailyhot("weibo")
               results = []
               for item in raw_items:
                   results.append(HotTopic(
                       title=item.get("title", ""),
                       url=item.get("url", ""),
                       source="weibo",
                       hot_value=str(item.get("hot", "")),
                       category="微博热搜",
                   ))
               return results

       register_crawler(WeiboCrawler())
       ```
    2. 创建 tests/test_adapters.py，使用 unittest.mock.patch 模拟 fetch_dailyhot：
       ```python
       """平台适配器单元测试"""
       from unittest.mock import patch
       import pytest
       from src.crawler.models import HotTopic

       MOCK_WEIBO_DATA = [
           {"title": "测试热搜1", "url": "https://weibo.com/1", "hot": "1000000", "desc": "描述1"},
           {"title": "测试热搜2", "url": "https://weibo.com/2", "hot": "500000", "desc": "描述2"},
       ]

       class TestWeiboCrawler:
           @patch("src.crawler.weibo.fetch_dailyhot")
           def test_returns_hottopic_list(self, mock_fetch):
               mock_fetch.return_value = MOCK_WEIBO_DATA
               from src.crawler.weibo import WeiboCrawler
               crawler = WeiboCrawler()
               topics = crawler.get_hot_list()
               assert len(topics) == 2
               assert all(isinstance(t, HotTopic) for t in topics)
               assert topics[0].source == "weibo"
               assert topics[0].title == "测试热搜1"
               assert topics[0].category == "微博热搜"

           @patch("src.crawler.weibo.fetch_dailyhot")
           def test_empty_list(self, mock_fetch):
               mock_fetch.return_value = []
               from src.crawler.weibo import WeiboCrawler
               crawler = WeiboCrawler()
               assert crawler.get_hot_list() == []
       ```
  </action>
  <verify>
    <automated>cd /Users/wencai/github/newsAgent && python -m pytest tests/test_adapters.py::TestWeiboCrawler -x -v</automated>
  </verify>
  <done>
    src/crawler/weibo.py 存在，WeiboCrawler.get_hot_list() 返回 list[HotTopic]，source="weibo"，调用 fetch_dailyhot("weibo")。模块末尾 register_crawler(WeiboCrawler())。TestWeiboCrawler 2 个测试通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建知乎 ZhihuCrawler + 百度 BaiduCrawler 适配器</name>
  <read_first>
    src/crawler/weibo.py (Task 1 创建的适配器模式)
    tests/test_adapters.py (Task 1 创建的测试文件)
  </read_first>
  <files>src/crawler/zhihu.py, src/crawler/baidu.py, tests/test_adapters.py</files>
  <action>
    1. 创建 src/crawler/zhihu.py（per D-12），结构与 weibo.py 完全一致，差异点：
       - fetch_dailyhot("zhihu")
       - source="zhihu"
       - category="知乎热榜"
       - register_crawler(ZhihuCrawler())
    2. 创建 src/crawler/baidu.py（per D-13），结构与 weibo.py 完全一致，差异点：
       - fetch_dailyhot("baidu")
       - source="baidu"
       - category="百度热搜"
       - register_crawler(BaiduCrawler())
    3. 在 tests/test_adapters.py 中追加 TestZhihuCrawler 和 TestBaiduCrawler 测试类，每个类包含：
       - test_returns_hottopic_list: mock fetch_dailyhot 返回测试数据，验证 source 和 category
       - test_empty_list: mock 返回空列表，验证返回空
  </action>
  <verify>
    <automated>cd /Users/wencai/github/newsAgent && python -m pytest tests/test_adapters.py -x -v</automated>
  </verify>
  <done>
    src/crawler/zhihu.py 和 src/crawler/baidu.py 存在，各自 get_hot_list() 返回 list[HotTopic]，source 分别为 "zhihu" 和 "baidu"。tests/test_adapters.py 全部 6 个测试通过（每个适配器 2 个）。
  </done>
</task>

</tasks>

<verification>
```bash
# 所有适配器测试通过
python -m pytest tests/test_adapters.py -x -v

# 适配器导入并自动注册
python -c "
import src.crawler.weibo
import src.crawler.zhihu
import src.crawler.baidu
from src.crawler import get_registered_crawlers
crawlers = get_registered_crawlers()
names = [type(c).__name__ for c in crawlers]
assert 'WeiboCrawler' in names
assert 'ZhihuCrawler' in names
assert 'BaiduCrawler' in names
print(f'All 3 adapters registered: {names}')
"
```
</verification>

<success_criteria>
- src/crawler/weibo.py 存在，WeiboCrawler 调用 fetch_dailyhot("weibo")，source="weibo"，category="微博热搜"
- src/crawler/zhihu.py 存在，ZhihuCrawler 调用 fetch_dailyhot("zhihu")，source="zhihu"，category="知乎热榜"
- src/crawler/baidu.py 存在，BaiduCrawler 调用 fetch_dailyhot("baidu")，source="baidu"，category="百度热搜"
- 三个模块各自末尾调用 register_crawler()
- tests/test_adapters.py 包含 6 个测试（每个适配器 2 个），全部通过
</success_criteria>

<output>
完成后创建 .planning/phases/03-multi-source/03-04-SUMMARY.md
</output>
