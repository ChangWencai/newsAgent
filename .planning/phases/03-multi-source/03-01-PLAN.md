---
phase: 03-multi-source
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/crawler/models.py
  - src/crawler/protocol.py
  - src/crawler/__init__.py
  - src/storage/database.py
  - tests/test_models.py
  - tests/test_registry.py
autonomous: true
requirements:
  - MULTI-01
  - MULTI-02
  - MULTI-03

must_haves:
  truths:
    - "HotTopic frozen dataclass 可创建且字段不可变"
    - "CrawlerProtocol 定义了 get_hot_list 方法签名"
    - "register_crawler 可注册爬虫，get_registered_crawlers 可获取列表"
    - "Database.insert_topic 支持 source 参数"
    - "Database.hot_topics 表包含 source 列"
    - "Database.find_similar_topics 可按标题相似度查询"
  artifacts:
    - path: "src/crawler/models.py"
      provides: "HotTopic frozen dataclass"
      contains: "@dataclass(frozen=True)"
    - path: "src/crawler/protocol.py"
      provides: "CrawlerProtocol Protocol"
      contains: "class CrawlerProtocol(Protocol)"
    - path: "src/crawler/__init__.py"
      provides: "爬虫注册中心"
      exports: ["register_crawler", "get_registered_crawlers", "CRAWLERS"]
    - path: "src/storage/database.py"
      provides: "source 列 + find_similar_topics 方法"
      contains: "find_similar_topics"
    - path: "tests/test_models.py"
      provides: "HotTopic 单元测试"
    - path: "tests/test_registry.py"
      provides: "注册机制单元测试"
  key_links:
    - from: "src/crawler/__init__.py"
      to: "src/crawler/protocol.py"
      via: "import CrawlerProtocol"
      pattern: "from src.crawler.protocol import"
    - from: "src/crawler/models.py"
      to: "src/crawler/protocol.py"
      via: "import HotTopic"
      pattern: "from src.crawler.models import HotTopic"
    - from: "src/storage/database.py"
      to: "difflib"
      via: "difflib in find_similar_topics"
      pattern: "difflib.SequenceMatcher"
---

<objective>
创建多源爬虫基础设施：HotTopic 数据结构、CrawlerProtocol 协议接口、爬虫注册中心、Database source 列迁移。

Purpose: 为后续所有平台适配器提供统一的类型契约和注册机制，Database schema 变更在此阶段完成以避免后续冲突。
Output: models.py, protocol.py, __init__.py (注册中心), database.py (source 列 + find_similar_topics), 两组单元测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-source/03-CONTEXT.md
@.planning/phases/03-multi-source/03-RESEARCH.md
@src/storage/database.py

<interfaces>
From src/storage/database.py (现有接口):
```python
class Database:
    def insert_topic(self, title, url="", hot_value="", category="") -> int
    def topic_exists(self, title) -> bool
    def _execute_write(self, sql, params=()) -> cursor
    def _execute_read(self, sql, params=()) -> cursor
    def _init_tables(self) -> None
```

From tests/conftest.py (测试 fixture):
```python
@pytest.fixture
def db():
    database = Database(':memory:')
    yield database
    database._conn.close()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 HotTopic frozen dataclass 和 CrawlerProtocol 协议</name>
  <read_first>
    src/crawler/__init__.py (当前为空文件)
    rules/python/coding-style.md (frozen dataclass 模式)
    rules/python/patterns.md (Protocol 模式)
  </read_first>
  <files>src/crawler/models.py, src/crawler/protocol.py, tests/test_models.py</files>
  <behavior>
    - Test 1: HotTopic(title="t", url="u", source="s") 字段值正确
    - Test 2: 修改 frozen dataclass 字段抛出 FrozenInstanceError
    - Test 3: 默认字段 hot_value="", category="", fetched_at=""
    - Test 4: 两个相同值 HotTopic 相等
  </behavior>
  <action>
    1. 创建 `src/crawler/models.py`，定义 HotTopic frozen dataclass（per D-03, D-04, D-05）：
       ```python
       """统一热点数据结构"""
       from dataclasses import dataclass

       @dataclass(frozen=True)
       class HotTopic:
           title: str
           url: str
           source: str
           hot_value: str = ""
           category: str = ""
           fetched_at: str = ""
       ```
    2. 创建 `src/crawler/protocol.py`，定义 CrawlerProtocol（per D-01, D-02）：
       ```python
       """爬虫协议接口定义"""
       from typing import Protocol
       from src.crawler.models import HotTopic

       class CrawlerProtocol(Protocol):
           def get_hot_list(self) -> list[HotTopic]: ...
       ```
    3. 创建 `tests/test_models.py`，验证 HotTopic frozen dataclass 行为：
       - 可正常创建，title/url/source 字段值正确
       - frozen=True 时修改字段抛出 FrozenInstanceError
       - 默认参数 hot_value/category/fetched_at 为空字符串
       - 两个相同字段值的 HotTopic 使用 == 比较为 True
  </action>
  <verify>
    <automated>cd /Users/wencai/github/newsAgent && python -m pytest tests/test_models.py -x -v</automated>
  </verify>
  <done>
    src/crawler/models.py 包含 @dataclass(frozen=True) class HotTopic，6 个 str 字段。src/crawler/protocol.py 包含 class CrawlerProtocol(Protocol) 声明 get_hot_list 方法。tests/test_models.py 4 个测试全部通过。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建爬虫注册中心</name>
  <read_first>
    src/crawler/__init__.py (当前为空文件)
    src/crawler/protocol.py (Task 1 创建的 CrawlerProtocol)
  </read_first>
  <files>src/crawler/__init__.py, tests/test_registry.py</files>
  <behavior>
    - Test 1: 初始 get_registered_crawlers() 返回 []
    - Test 2: register_crawler(mock) 后 get_registered_crawlers() 包含该实例
    - Test 3: get_registered_crawlers 返回的是副本，修改不影响 CRAWLERS 原列表
    - Test 4: 注册 3 个爬虫后返回 3 个，顺序一致
  </behavior>
  <action>
    1. 在 `src/crawler/__init__.py` 中实现注册中心（per D-06, D-07）：
       ```python
       """爬虫注册中心 — 模块级 CRAWLERS 列表 + 注册/查询函数"""
       from src.crawler.protocol import CrawlerProtocol, HotTopic

       CRAWLERS: list[CrawlerProtocol] = []

       def register_crawler(crawler: CrawlerProtocol) -> None:
           """注册一个爬虫实例到全局列表"""
           CRAWLERS.append(crawler)

       def get_registered_crawlers() -> list[CrawlerProtocol]:
           """返回所有已注册爬虫的副本（不影响原列表）"""
           return list(CRAWLERS)

       __all__ = ["CRAWLERS", "register_crawler", "get_registered_crawlers", "HotTopic", "CrawlerProtocol"]
       ```
    2. 创建 `tests/test_registry.py`，验证注册机制：
       - 初始状态 CRAWLERS 为空列表
       - register_crawler 后 get_registered_crawlers 返回已注册实例
       - get_registered_crawlers 返回的是副本（对返回列表 append 不影响 CRAWLERS 长度）
       - 注册多个爬虫后返回列表长度和顺序正确
  </action>
  <verify>
    <automated>cd /Users/wencai/github/newsAgent && python -m pytest tests/test_registry.py -x -v</automated>
  </verify>
  <done>
    src/crawler/__init__.py 导出 CRAWLERS、register_crawler、get_registered_crawlers。tests/test_registry.py 4 个测试通过。模块可正常导入: `python -c "from src.crawler import register_crawler, get_registered_crawlers"`。
  </done>
</task>

<task type="auto">
  <name>Task 3: Database source 列迁移 + find_similar_topics 方法</name>
  <read_first>
    src/storage/database.py (当前 _init_tables 第32-65行, insert_topic 第73-79行)
  </read_first>
  <files>src/storage/database.py</files>
  <action>
    1. 修改 `_init_tables()` 方法（第32-65行），在 `self._conn.commit()` 之前添加 source 列迁移（per RESEARCH.md Pitfall 4）：
       ```python
       # source 列迁移（已有表兼容）
       try:
           self._execute_write("ALTER TABLE hot_topics ADD COLUMN source TEXT DEFAULT ''")
       except Exception:
           pass  # 列已存在时忽略
       ```
    2. 修改 `insert_topic()` 方法签名（第73行），在 category 参数后添加 source 参数：
       - 旧签名: `def insert_topic(self, title, url="", hot_value="", category="")`
       - 新签名: `def insert_topic(self, title, url="", hot_value="", category="", source="")`
       - SQL 改为: `INSERT INTO hot_topics (title, url, hot_value, category, source, fetched_at) VALUES (?, ?, ?, ?, ?, ?)`
       - params 元组改为: `(title, url, hot_value, category, source, now)`
    3. 在 `insert_topic` 方法之后新增 `find_similar_topics` 方法（per D-17）：
       ```python
       def find_similar_topics(self, title: str, threshold: float = 0.6) -> list[dict]:
           """查询已存在话题中与给定标题相似的记录"""
           import difflib
           rows = self._execute_read("SELECT * FROM hot_topics").fetchall()
           similar = []
           for row in rows:
               ratio = difflib.SequenceMatcher(None, title, row["title"]).ratio()
               if ratio >= threshold:
                   similar.append(dict(row))
           return similar
       ```
  </action>
  <verify>
    <automated>cd /Users/wencai/github/newsAgent && python -m pytest tests/test_database.py -x -v</automated>
  </verify>
  <done>
    _init_tables 包含 ALTER TABLE source 迁移（已有列不报错）。insert_topic 接受 source 参数并写入 hot_topics.source 列。find_similar_topics 方法可按相似度阈值查询已有话题。所有已有 test_database.py 测试继续通过（insert_topic 签名向后兼容：source 有默认值 ""）。
  </done>
</task>

</tasks>

<verification>
整体验证：
```bash
# 新测试全部通过
python -m pytest tests/test_models.py tests/test_registry.py -x -v

# 已有测试无回归
python -m pytest tests/test_database.py tests/test_pipeline.py -x -v

# 模块导入正常
python -c "from src.crawler import register_crawler, get_registered_crawlers, HotTopic; print('registry OK')"
python -c "from src.crawler.protocol import CrawlerProtocol; print('protocol OK')"
python -c "from src.crawler.models import HotTopic; h = HotTopic(title='t', url='u', source='s'); print(f'model OK: {h.title}')"
```
</verification>

<success_criteria>
- src/crawler/models.py 存在，包含 @dataclass(frozen=True) class HotTopic，6 个 str 字段（title, url, source, hot_value, category, fetched_at）
- src/crawler/protocol.py 存在，包含 class CrawlerProtocol(Protocol)，声明 get_hot_list -> list[HotTopic]
- src/crawler/__init__.py 包含 CRAWLERS 列表、register_crawler()、get_registered_crawlers() 三个导出
- src/storage/database.py _init_tables 包含 ALTER TABLE hot_topics ADD COLUMN source
- src/storage/database.py insert_topic 签名包含 source="" 参数
- src/storage/database.py 包含 find_similar_topics(self, title: str, threshold: float = 0.6) -> list[dict]
- tests/test_models.py 4 个测试通过
- tests/test_registry.py 4 个测试通过
- tests/test_database.py 全部已有测试通过（无回归）
- 88 个已有测试无回归
</success_criteria>

<output>
完成后创建 `.planning/phases/03-multi-source/03-01-SUMMARY.md`
</output>
