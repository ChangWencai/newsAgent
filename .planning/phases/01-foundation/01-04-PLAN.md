---
phase: 01-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 03
files_modified:
  - src/scheduler/jobs.py
  - publish.py
  - tests/test_pipeline.py
autonomous: true
requirements:
  - FOUND-01
user_setup: []

must_haves:
  truths:
    - "run_pipeline 重构为 create_pipeline(crawler, writer, db) 构造函数注入"
    - "函数内部不再硬编码实例化 DouyinCrawler 和 ArticleGenerator"
    - "publish.py 使用 Database 方法替代直接 SQL"
    - "test_pipeline.py 用 mock 验证编排逻辑"
  artifacts:
    - path: "src/scheduler/jobs.py"
      provides: "create_pipeline 工厂函数"
      contains: "def create_pipeline(crawler, writer, db)"
    - path: "publish.py"
      provides: "使用 Database 方法的发布脚本"
    - path: "tests/test_pipeline.py"
      provides: "Pipeline 编排测试"
      contains: "test_create_pipeline"
  key_links:
    - from: "src/scheduler/jobs.py"
      to: "crawler/writer 参数"
      via: "构造函数注入，不再内部实例化"
      pattern: "crawler\\.get_hot_list|writer\\.generate_article"
    - from: "main.py"
      to: "src/scheduler/jobs.py"
      via: "create_pipeline 返回函数传给 scheduler"
      pattern: "create_pipeline"
---

<objective>
重构 run_pipeline 为构造函数注入模式，消除函数内部的硬编码依赖实例化。

Purpose: 当前 run_pipeline(db) 在函数内部直接实例化 DouyinCrawler() 和 ArticleGenerator()，无法在测试中替换为 mock。重构为 create_pipeline(crawler, writer, db) 后，测试可注入 mock，publish.py 中的直接 SQL 也替换为 Database 方法。
Output: 重构后的 src/scheduler/jobs.py、更新后的 publish.py、tests/test_pipeline.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@src/scheduler/jobs.py
@publish.py
@src/crawler/tophub.py
@src/writer/generator.py

<interfaces>
当前 run_pipeline(db) 的内部依赖（需要外部注入）：
- DouyinCrawler() — src/crawler/tophub.py，方法 get_hot_list() -> list[dict]
- ArticleGenerator() — src/writer/generator.py，方法 generate_article(title, style) -> dict | None

publish.py 中的直接 SQL（需替换）：
第 64-69 行：conn.execute("SELECT * FROM articles WHERE id = ?", (aid,)) -> db.get_article(aid)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重构 jobs.py 为 create_pipeline 构造函数注入</name>
  <read_first>
    /Users/wencai/github/newsAgent/src/scheduler/jobs.py
  </read_first>
  <files>src/scheduler/jobs.py</files>
  <action>
    重构 src/scheduler/jobs.py：
    1. 新增 create_pipeline(crawler, writer, db) 工厂函数，返回可调用的 pipeline 函数
    2. 将原有 run_pipeline 逻辑移入 _run_pipeline_inner(crawler, writer, db)，将内部的 DouyinCrawler() 和 ArticleGenerator() 替换为参数 crawler 和 writer
    3. 保留 run_pipeline(db) 作为向后兼容入口，内部创建默认依赖后调用 create_pipeline：
       ```python
       def create_pipeline(crawler, writer, db):
           """创建 pipeline 函数，通过构造函数注入依赖"""
           def pipeline():
               _run_pipeline_inner(crawler, writer, db)
           return pipeline

       def _run_pipeline_inner(crawler, writer, db):
           """执行完整的新闻生产流水线"""
           logger.info("开始执行新闻生产流水线...")
           topics = crawler.get_hot_list()
           if not topics:
               logger.warning("未获取到热点数据")
               return
           logger.info("获取到 %d 条热点", len(topics))
           new_topics = []
           for topic in topics:
               if not db.topic_exists(topic["title"]):
                   new_topics.append(topic)
               if len(new_topics) >= MAX_TOPICS_PER_RUN:
                   break
           if not new_topics:
               logger.info("没有新的热点需要处理")
               return
           logger.info("过滤后有 %d 条新热点待处理", len(new_topics))
           for topic in new_topics:
               try:
                   topic_id = db.insert_topic(
                       title=topic["title"], url=topic["url"],
                       hot_value=topic["hot_value"], category=topic["category"],
                   )
                   article = writer.generate_article(
                       title=topic["title"], style=DEFAULT_STYLE,
                   )
                   if not article:
                       logger.warning("文章生成失败: %s", topic["title"])
                       continue
                   db.insert_article(
                       topic_id=topic_id, title=article["title"],
                       content=article["content"], style=article["style"],
                   )
                   logger.info("已生成文章: %s", article["title"])
               except Exception as e:
                   logger.error("处理热点失败 [%s]: %s", topic["title"], e)
           logger.info("本次流水线执行完成")

       def run_pipeline(db):
           """兼容入口：创建默认依赖并执行"""
           crawler = DouyinCrawler()
           writer = ArticleGenerator()
           pipeline = create_pipeline(crawler, writer, db)
           pipeline()
       ```
  </action>
  <verify>
    <automated>cd /Users/wencai/github/newsAgent && python -c "
from src.scheduler.jobs import create_pipeline, run_pipeline
import inspect
sig = inspect.signature(create_pipeline)
assert list(sig.parameters.keys()) == ['crawler', 'writer', 'db']
sig2 = inspect.signature(run_pipeline)
assert 'db' in sig2.parameters
print('OK: create_pipeline(crawler, writer, db) + run_pipeline(db) backward compatible')
"</automated>
  </verify>
  <done>
    - create_pipeline(crawler, writer, db) 函数存在且签名正确
    - _run_pipeline_inner 使用注入的 crawler.get_hot_list() 和 writer.generate_article()
    - run_pipeline(db) 兼容入口保留
  </done>
</task>

<task type="auto">
  <name>Task 2: 更新 publish.py 使用 Database 方法</name>
  <read_first>
    /Users/wencai/github/newsAgent/publish.py
  </read_first>
  <files>publish.py</files>
  <action>
    将 publish.py 第 63-69 行的直接 SQL 替换为 db.get_article(aid)：
    旧代码：
      conn = db._get_conn()
      for aid in article_ids:
          row = conn.execute("SELECT * FROM articles WHERE id = ?", (aid,)).fetchone()
          if row:
              articles.append(dict(row))
      conn.close()
    新代码：
      for aid in article_ids:
          article = db.get_article(aid)
          if article:
              articles.append(article)
  </action>
  <verify>
    <automated>cd /Users/wencai/github/newsAgent && grep -c "SELECT\|_get_conn\|conn.execute" publish.py</automated>
  </verify>
  <done>
    - publish.py 中无直接 SQL 和 _get_conn 调用
    - 使用 db.get_article(aid) 获取文章
  </done>
</task>

<task type="auto">
  <name>Task 3: 编写 Pipeline 单元测试</name>
  <read_first>
    /Users/wencai/github/newsAgent/src/scheduler/jobs.py
    /Users/wencai/github/newsAgent/tests/conftest.py
  </read_first>
  <files>tests/test_pipeline.py</files>
  <action>
    创建 tests/test_pipeline.py，使用 unittest.mock 验证编排逻辑：
    - test_returns_callable: create_pipeline 返回 callable
    - test_pipeline_no_topics: crawler 返回空列表时不调用 writer
    - test_pipeline_dedup: 已存在热点被跳过，新热点被处理
    - test_pipeline_full_flow: 完整流程验证数据入库（1 热点 -> 1 文章）
    - test_pipeline_writer_failure_continues: writer 返回 None 时该条跳过但后续继续
  </action>
  <verify>
    <automated>cd /Users/wencai/github/newsAgent && python -m pytest tests/test_pipeline.py -v</automated>
  </verify>
  <done>
    - tests/test_pipeline.py 包含 5 个测试
    - 全部测试通过
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/wencai/github/newsAgent && python -m pytest tests/ -v
# 预期：test_smoke (3) + test_database (>12) + test_routes (>12) + test_pipeline (5) 全部通过
```

```bash
grep -n "SELECT\|_get_conn\|conn.execute" /Users/wencai/github/newsAgent/publish.py || echo "PASS: publish.py zero SQL"
```
</verification>

<success_criteria>
- create_pipeline(crawler, writer, db) 工厂函数存在
- run_pipeline(db) 向后兼容入口保留
- publish.py 使用 db.get_article(aid) 替代直接 SQL
- tests/test_pipeline.py 全部通过
</success_criteria>

<output>
完成后创建 `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
