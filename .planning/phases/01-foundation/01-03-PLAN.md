---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 02
files_modified:
  - src/web/routes/__init__.py
  - src/web/routes/web.py
  - src/web/routes/api.py
  - main.py
  - tests/test_routes.py
  - tests/conftest.py
autonomous: true
requirements:
  - FOUND-01
  - FOUND-03
user_setup: []

must_haves:
  truths:
    - "Flask app 通过 create_app(db=None, db_path=None) 工厂函数创建"
    - "模块级 _db 全局变量完全消除"
    - "web_bp 包含 dashboard/topics/articles/detail/settings 视图路由"
    - "api_bp 包含 run-pipeline/delete/rss/health API 路由"
    - "Blueprint 工厂函数通过闭包注入 db"
    - "所有模板 URL 硬编码路径保持不变"
    - "test_routes.py 验证所有路由正常响应"
  artifacts:
    - path: "src/web/routes/__init__.py"
      provides: "导出 create_web_bp 和 create_api_bp"
      exports: ["create_web_bp", "create_api_bp"]
    - path: "src/web/routes/web.py"
      provides: "Web 视图路由"
      contains: "def create_web_bp(db)"
    - path: "src/web/routes/api.py"
      provides: "API 路由 + RSS 构建"
      contains: "def create_api_bp(db)"
    - path: "main.py"
      provides: "Application Factory"
      contains: "def create_app"
    - path: "tests/test_routes.py"
      provides: "路由集成测试"
      contains: "test_dashboard_returns_200"
    - path: "tests/conftest.py"
      provides: "app 和 client fixture"
      exports: ["app", "client"]
  key_links:
    - from: "main.py"
      to: "src/web/routes/__init__.py"
      via: "from src.web.routes import create_web_bp, create_api_bp"
      pattern: "from src.web.routes import"
    - from: "src/web/routes/web.py"
      to: "Database 方法"
      via: "闭包捕获 db，调用 db.get_dashboard_stats 等"
      pattern: "db\\.(get_dashboard_stats|get_topics|get_articles|get_article)"
    - from: "src/web/routes/api.py"
      to: "src/scheduler/jobs.py"
      via: "run_pipeline(db)"
      pattern: "run_pipeline\\(db\\)"
---

<objective>
实现 Flask Application Factory 模式和 Blueprint 拆分，消除模块级 _db 全局变量。

Purpose: 当前 routes.py 使用模块级 _db 全局变量和 init_web() 注册路由，无法独立测试。重构为 create_app() 工厂 + Blueprint 工厂后，测试可注入 mock db，路由按 web/api 功能域拆分。
Output: src/web/routes/ 包（__init__.py + web.py + api.py）、重构后的 main.py、tests/test_routes.py、更新后的 tests/conftest.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@main.py
@src/web/routes.py
@src/publisher/rss_feed.py
@src/storage/database.py

<interfaces>
当前 routes.py 的 7 个路由（全部需要迁移）：

视图路由（返回 HTML）：
  /                    -> dashboard()        -> index.html
  /topics              -> topics_list()      -> topics.html
  /articles            -> articles_list()    -> articles.html
  /article/<id>        -> article_detail()   -> article_detail.html
  /settings            -> settings_page()    -> settings.html

API 路由（返回 JSON/RSS）：
  POST /api/run-pipeline     -> api_run_pipeline()
  POST /api/article/<id>/delete -> api_delete_article()
  GET /rss                   -> rss_feed()（来自 rss_feed.py）
  GET /health                -> health()（来自 rss_feed.py）

模板 URL 全部硬编码（href="/" 等），无需修改。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 Blueprint 工厂函数（web.py + api.py + __init__.py）</name>
  <read_first>
    /Users/wencai/github/newsAgent/src/web/routes.py
    /Users/wencai/github/newsAgent/src/publisher/rss_feed.py
    /Users/wencai/github/newsAgent/src/scheduler/jobs.py
  </read_first>
  <files>src/web/routes/__init__.py, src/web/routes/web.py, src/web/routes/api.py</files>
  <action>
    1. 备份旧文件：将 src/web/routes.py 重命名为 src/web/routes_old.py
    2. 创建 src/web/routes/ 目录
    3. 创建 src/web/routes/__init__.py：
       ```python
       """Blueprint 工厂函数导出"""
       from src.web.routes.web import create_web_bp
       from src.web.routes.api import create_api_bp
       ```

    4. 创建 src/web/routes/web.py：
       - 从 routes.py 迁移 5 个视图路由（dashboard/topics/articles/detail/settings）
       - create_web_bp(db) 工厂函数，返回 Blueprint
       - url_prefix=""（保持原有 URL 路径不变）
       - 所有 SQL 替换为 db 方法调用：
         * dashboard() -> db.get_dashboard_stats()
         * topics_list() -> db.get_topics(limit=50)
         * articles_list() -> db.get_articles(status=...)
         * article_detail() -> db.get_article(article_id)
         * settings_page() 保持不变（仅读取 config）

    5. 创建 src/web/routes/api.py：
       - 从 routes.py 迁移 api_run_pipeline 和 api_delete_article
       - 从 rss_feed.py 迁移 rss_feed、health、_build_rss_xml、_content_to_html、_format_rfc822
       - create_api_bp(db) 工厂函数，返回 Blueprint
       - api_delete_article 使用 db.get_article() 检查存在性 + db.delete_article() 删除
       - url_prefix=""（保持原有 API URL 路径不变）
  </action>
  <verify>
    <automated>cd /Users/wencai/github/newsAgent && python -c "
from src.web.routes import create_web_bp, create_api_bp
from src.storage.database import Database
db = Database(':memory:')
web_bp = create_web_bp(db)
api_bp = create_api_bp(db)
assert web_bp.name == 'web'
assert api_bp.name == 'api'
print('Blueprint factories OK')
db._conn.close()
"</automated>
  </verify>
  <done>
    - src/web/routes/__init__.py 导出 create_web_bp 和 create_api_bp
    - web.py 包含 5 个视图路由，全部调用 db 方法，零 SQL
    - api.py 包含 4 个 API 路由，RSS 函数从 rss_feed.py 迁移
    - Blueprint 导入和实例化成功
  </done>
</task>

<task type="auto">
  <name>Task 2: 重构 main.py 为 Application Factory</name>
  <read_first>
    /Users/wencai/github/newsAgent/main.py
    /Users/wencai/github/newsAgent/src/web/routes.py
  </read_first>
  <files>main.py</files>
  <action>
    重构 main.py：
    1. create_app 改为接受 db 和 db_path 参数：
       ```python
       def create_app(db=None, db_path=None):
           """Application Factory：创建 Flask 应用"""
           app = Flask(
               __name__,
               template_folder="src/web/templates",
               static_folder="src/web/static",
           )
           if db is None:
               from config.settings import DB_PATH
               from src.storage.database import Database
               db = Database(db_path or DB_PATH)
           from src.web.routes import create_web_bp, create_api_bp
           app.register_blueprint(create_web_bp(db))
           app.register_blueprint(create_api_bp(db))
           return app
       ```
       这样 gunicorn main:create_app 无参调用时内部创建 db。
       main() 中 create_app(db=db) 时复用同一 db 实例。

    2. main() 函数更新：
       - 创建 db = Database(DB_PATH)
       - app = create_app(db=db)
       - scheduler.add_job(run_pipeline, args=[db]) 使用同一 db
       - 删除旧的 init_web() 和 create_rss_app() 调用

    3. 删除旧 import：
       - 删除 from src.publisher.rss_feed import create_rss_app
       - 删除 from src.web.routes import init_web
  </action>
  <verify>
    <automated>cd /Users/wencai/github/newsAgent && python -c "
from main import create_app
import inspect
sig = inspect.signature(create_app)
params = list(sig.parameters.keys())
assert 'db' in params
assert 'db_path' in params
# 无参调用（gunicorn 模式）
app = create_app()
assert app is not None
print(f'create_app() OK, type={type(app).__name__}')
"</automated>
  </verify>
  <done>
    - create_app(db=None, db_path=None) 存在
    - 无参调用 create_app() 返回 Flask app（gunicorn 兼容）
    - create_app(db=db) 使用注入的 db 实例
    - 旧的 init_web() 和 create_rss_app() 合并逻辑已删除
    - main() 使用 create_app(db=db) 创建 app
  </done>
</task>

<task type="auto">
  <name>Task 3: 更新 conftest.py 和编写路由集成测试</name>
  <read_first>
    /Users/wencai/github/newsAgent/tests/conftest.py
  </read_first>
  <files>tests/conftest.py, tests/test_routes.py</files>
  <action>
    1. 更新 tests/conftest.py，添加 app 和 client fixture：
       ```python
       @pytest.fixture
       def app(db):
           from main import create_app
           app = create_app(db=db)
           app.config['TESTING'] = True
           return app

       @pytest.fixture
       def client(app):
           return app.test_client()
       ```

    2. 创建 tests/test_routes.py，包含以下测试类：
       - TestDashboardRoute: GET / 返回 200，包含统计数据
       - TestTopicsRoute: GET /topics 返回 200
       - TestArticlesRoute: GET /articles 返回 200，status filter 生效
       - TestArticleDetailRoute: GET /article/<id> 返回 200 或 404
       - TestSettingsRoute: GET /settings 返回 200
       - TestApiRunPipeline: POST /api/run-pipeline 返回 JSON 含 success
       - TestApiDeleteArticle: POST /api/article/<id>/delete 正确删除，不存在返回 404
       - TestRssRoute: GET /rss 返回 XML 含 rss 标签
       - TestHealthRoute: GET /health 返回 {"status": "ok"}
  </action>
  <verify>
    <automated>cd /Users/wencai/github/newsAgent && python -m pytest tests/test_routes.py -v</automated>
  </verify>
  <done>
    - tests/conftest.py 包含 app 和 client fixture
    - tests/test_routes.py 包含 9 个测试类
    - 所有路由测试通过
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/wencai/github/newsAgent && python -m pytest tests/ -v
# 预期：test_smoke (3) + test_database (>12) + test_routes (>12) 全部通过
```

```bash
# 验证 _db 全局变量已消除
grep -rn "^_db\|global _db" /Users/wencai/github/newsAgent/src/web/routes/ 2>/dev/null || echo "PASS: No global _db"
```

```bash
# 验证路由层零 SQL
grep -rn "SELECT\|INSERT\|UPDATE\|DELETE\|_get_conn" /Users/wencai/github/newsAgent/src/web/routes/web.py 2>/dev/null || echo "PASS: No SQL in web.py"
grep -rn "SELECT\|INSERT\|UPDATE\|DELETE\|_get_conn" /Users/wencai/github/newsAgent/src/web/routes/api.py 2>/dev/null || echo "PASS: No SQL in api.py"
```
</verification>

<success_criteria>
- create_app(db=None, db_path=None) 存在，gunicorn main:create_app 可无参调用
- src/web/routes/web.py 5 个视图路由，零 SQL
- src/web/routes/api.py 4 个 API 路由，RSS 函数迁移自 rss_feed.py
- 模块级 _db 全局变量完全消除
- 模板 URL 路径无需修改
- tests/test_routes.py 全部通过
</success_criteria>

<output>
完成后创建 `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
