---
phase: 01-foundation
plan: 05
type: execute
wave: 5
depends_on:
  - 04
files_modified:
  - gunicorn.conf.py
  - main.py
  - requirements.txt
autonomous: true
requirements:
  - FOUND-06
user_setup: []

must_haves:
  truths:
    - "gunicorn.conf.py 配置文件存在，bind/workers/threads/log 参数集中管理"
    - "gunicorn -c gunicorn.conf.py main:create_app 可启动应用"
    - "单 worker + 4 threads 配置避免 APScheduler 重复调度"
    - "atexit handler 注册 scheduler.shutdown(wait=True) 安全退出"
    - "SIGTERM/SIGINT 信号处理确保 pipeline 任务安全退出"
    - "main.py 兼容 gunicorn 模式和 python main.py 开发模式"
    - "requirements.txt 包含 gunicorn"
  artifacts:
    - path: "gunicorn.conf.py"
      provides: "gunicorn 配置文件"
      contains: "workers = 1"
    - path: "main.py"
      provides: "信号处理 + atexit + 双模式兼容"
      contains: "atexit"
    - path: "requirements.txt"
      provides: "更新后的依赖列表"
      contains: "gunicorn"
  key_links:
    - from: "main.py"
      to: "gunicorn"
      via: "create_app 暴露为模块级函数供 gunicorn 导入"
      pattern: "def create_app"
    - from: "main.py"
      to: "scheduler.shutdown"
      via: "atexit handler 确保退出时安全关闭"
      pattern: "atexit\\.register.*scheduler"
    - from: "gunicorn.conf.py"
      to: "main:create_app"
      via: "wsgi_callable 配置"
      pattern: "wsgi_callable"
---

<objective>
配置 gunicorn 替代 Werkzeug 开发服务器用于生产部署，添加信号处理确保安全退出。

Purpose: 当前使用 Flask app.run() 开发服务器，单线程、无信号处理、不适合生产。gunicorn 单 worker + 多线程 + atexit/signal 处理后，Web 并发能力提升，scheduler 安全退出不丢任务。
Output: gunicorn.conf.py、更新后的 main.py（信号处理 + 双模式兼容）、更新后的 requirements.txt
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@main.py
@requirements.txt

<interfaces>
main.py 当前结构（Wave 3 已重构）：
- create_app(db=None, db_path=None) — Application Factory，模块级函数
- main() — argparse + Database + APScheduler + Flask app.run()

gunicorn 启动方式：gunicorn -c gunicorn.conf.py main:create_app
要求：create_app 必须是模块级可导入函数（Wave 3 已满足）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 gunicorn.conf.py 配置文件</name>
  <read_first>
    /Users/wencai/github/newsAgent/config/settings.py
  </read_first>
  <files>gunicorn.conf.py</files>
  <action>
    创建 gunicorn.conf.py：
    ```python
    """gunicorn 配置文件"""
    from config.settings import RSS_HOST, RSS_PORT

    # 绑定地址
    bind = f"{RSS_HOST}:{RSS_PORT}"

    # 单 worker + 多线程（避免 APScheduler 重复调度）
    workers = 1
    threads = 4
    worker_class = "gthread"

    # 日志
    accesslog = "-"
    errorlog = "-"
    loglevel = "info"

    # 超时（pipeline 可能执行较长时间）
    timeout = 300
    graceful_timeout = 30

    # 进程名称
    proc_name = "newsagent"
    ```
    注意：workers=1 是关键，避免多个 worker 各自启动 APScheduler。
  </action>
  <verify>
    <automated>cd /Users/wencai/github/newsAgent && python -c "
import importlib.util
spec = importlib.util.spec_from_file_location('gc', 'gunicorn.conf.py')
mod = importlib.util.module_from_spec(spec)
spec.loader.exec_module(mod)
assert mod.workers == 1
assert mod.threads == 4
assert mod.worker_class == 'gthread'
print(f'OK: workers={mod.workers}, threads={mod.threads}')
"</automated>
  </verify>
  <done>
    - gunicorn.conf.py 存在
    - workers=1, threads=4, worker_class="gthread"
    - bind 使用 config.settings 中的 RSS_HOST:RSS_PORT
  </done>
</task>

<task type="auto">
  <name>Task 2: 添加信号处理和双模式兼容到 main.py</name>
  <read_first>
    /Users/wencai/github/newsAgent/main.py
  </read_first>
  <files>main.py</files>
  <action>
    更新 main.py：
    1. 文件头部添加 import atexit 和 import signal
    2. main() 函数中 scheduler.start() 后添加：
       ```python
       # 安全退出：atexit handler
       atexit.register(lambda: scheduler.shutdown(wait=True))

       # 信号处理
       def _signal_handler(signum, frame):
           logger.info("收到信号 %d，正在关闭...", signum)
           scheduler.shutdown(wait=True)
           sys.exit(0)

       signal.signal(signal.SIGTERM, _signal_handler)
       signal.signal(signal.SIGINT, _signal_handler)
       ```
    3. 保持 create_app 是模块级函数不变
    4. 保持 if __name__ == "__main__": main() 不变
  </action>
  <verify>
    <automated>cd /Users/wencai/github/newsAgent && python -c "
from main import create_app
import main
assert callable(main.create_app)
assert callable(main.main)
import atexit, signal
print('main.py gunicorn compatible OK')
"</automated>
  </verify>
  <done>
    - main.py 包含 import atexit 和 import signal
    - atexit.register 已注册 scheduler.shutdown
    - SIGTERM/SIGINT 信号处理器已注册
    - create_app 是模块级函数
  </done>
</task>

<task type="auto">
  <name>Task 3: 更新 requirements.txt 添加 gunicorn</name>
  <read_first>
    /Users/wencai/github/newsAgent/requirements.txt
  </read_first>
  <files>requirements.txt</files>
  <action>
    在 requirements.txt 末尾添加 gunicorn>=23.0.0
  </action>
  <verify>
    <automated>cd /Users/wencai/github/newsAgent && grep "gunicorn" requirements.txt</automated>
  </verify>
  <done>
    - requirements.txt 包含 gunicorn>=23.0.0
  </done>
</task>

</tasks>

<verification>
```bash
# 验证 gunicorn 可启动（5 秒超时测试）
cd /Users/wencai/github/newsAgent && timeout 5 gunicorn -c gunicorn.conf.py main:create_app 2>&1 || true
# 预期：看到 "Booting worker with pid"，无 ImportError
```

```bash
# 验证开发模式仍可用
cd /Users/wencai/github/newsAgent && python -c "from main import create_app; app = create_app(); print('Dev mode OK')"
```

```bash
# 运行全部测试
cd /Users/wencai/github/newsAgent && python -m pytest tests/ -v
# 预期：全部通过
```
</verification>

<success_criteria>
- gunicorn.conf.py 存在，workers=1, threads=4
- main.py 注册 atexit handler 和 SIGTERM/SIGINT 信号处理器
- gunicorn -c gunicorn.conf.py main:create_app 可启动
- python main.py 开发模式仍正常工作
- requirements.txt 包含 gunicorn>=23.0.0
- 所有测试通过
</success_criteria>

<output>
完成后创建 `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
