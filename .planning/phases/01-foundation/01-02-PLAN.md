---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/storage/database.py
  - tests/conftest.py
  - tests/test_database.py
autonomous: true
requirements:
  - FOUND-02
  - FOUND-04
user_setup: []

must_haves:
  truths:
    - "Database 实例持有单一 SQLite 连接，不再每次操作新建/关闭"
    - "写操作通过 threading.Lock 序列化，WAL 模式支持并发读"
    - "Database 类补齐 get_dashboard_stats/get_topics/get_articles/get_article/delete_article 五个方法"
    - "已有方法（topic_exists/insert_topic 等）保持向后兼容"
    - "routes.py 中的所有 SQL 可以替换为 Database 方法调用"
    - "test_database.py 验证所有 Database 方法和并发安全性"
  artifacts:
    - path: "src/storage/database.py"
      provides: "重构后的 Database 类"
      contains: "_write_lock = threading.Lock()"
      exports: ["get_dashboard_stats", "get_topics", "get_articles", "get_article", "delete_article"]
    - path: "tests/test_database.py"
      provides: "Database 类单元测试"
      contains: "test_concurrent_writes"
    - path: "tests/conftest.py"
      provides: "更新后的 db fixture（使用 :memory:）"
  key_links:
    - from: "src/storage/database.py"
      to: "threading.Lock"
      via: "_write_lock 保护所有写操作"
      pattern: "with self._write_lock:"
    - from: "src/storage/database.py"
      to: "PRAGMA journal_mode=WAL"
      via: "__init__ 中执行 WAL 模式"
      pattern: "PRAGMA journal_mode=WAL"
    - from: "tests/conftest.py"
      to: "src/storage/database.py"
      via: "db fixture 使用 :memory: 单连接"
      pattern: "Database\\(':memory:'\\)"
---

<objective>
重构 Database 类为单连接 + threading.Lock + WAL 模式，补齐所有缺失的数据访问方法，消除 routes.py 中的直接 SQL。

Purpose: 当前 Database 类每次操作新建/关闭连接（并发不安全），且缺少 5 个 routes.py 需要的方法。重构后：单连接 + Lock 解决并发死锁，补齐方法使路由层零 SQL。
Output: 重构后的 src/storage/database.py（单连接+Lock+WAL+11 个公开方法）、tests/test_database.py、更新后的 tests/conftest.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@src/storage/database.py

<interfaces>
当前 routes.py 中直接执行的 SQL（必须替换为 Database 方法）：

routes.py:43-54 (dashboard) — SELECT COUNT(*) FROM hot_topics, articles, articles WHERE status='draft'/'published', SELECT * FROM articles ORDER BY generated_at DESC LIMIT 10
routes.py:71-73 (topics_list) — SELECT * FROM hot_topics ORDER BY id DESC LIMIT 50
routes.py:85-92 (articles_list) — SELECT * FROM articles [WHERE status=?] ORDER BY generated_at DESC
routes.py:104-106 (article_detail) — SELECT * FROM articles WHERE id=?
routes.py:127-134 (api_delete_article) — SELECT id FROM articles WHERE id=?, DELETE FROM articles WHERE id=?

Database 当前公开方法（保持兼容）：
- topic_exists(title) -> bool
- insert_topic(title, url, hot_value, category) -> int
- insert_article(topic_id, title, content, style) -> int
- get_unpublished_articles(limit) -> list[dict]
- get_recent_articles(limit) -> list[dict]
- mark_published(article_id) -> None
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 重构 Database 为单连接 + Lock + WAL</name>
  <read_first>
    /Users/wencai/github/newsAgent/src/storage/database.py
  </read_first>
  <files>src/storage/database.py</files>
  <behavior>
    - __init__ 创建单一连接并缓存为 self._conn（不再每次新建）
    - __init__ 执行 PRAGMA journal_mode=WAL
    - __init__ 创建 self._write_lock = threading.Lock()
    - _execute_write(sql, params) 方法：with self._write_lock: cursor = self._conn.execute(sql, params); self._conn.commit(); return cursor
    - _execute_read(sql, params) 方法：return self._conn.execute(sql, params)
    - 所有已有方法（topic_exists/insert_topic/insert_article/get_unpublished_articles/get_recent_articles/mark_published）重构为使用 _execute_write/_execute_read
    - _get_conn() 方法保留但返回 self._conn（不再新建连接）
    - _init_tables() 改为直接使用 self._conn
  </behavior>
  <action>
    重构 src/storage/database.py，具体改动：
    1. 文件头部添加 import threading
    2. __init__ 方法改为：
       ```python
       def __init__(self, db_path):
           self.db_path = db_path
           if db_path != ':memory:':
               os.makedirs(os.path.dirname(db_path), exist_ok=True)
           self._conn = sqlite3.connect(db_path, check_same_thread=False)
           self._conn.row_factory = sqlite3.Row
           self._conn.execute("PRAGMA journal_mode=WAL")
           self._write_lock = threading.Lock()
           self._init_tables()
       ```
    3. 添加 _execute_write 和 _execute_read 辅助方法：
       ```python
       def _execute_write(self, sql, params=()):
           with self._write_lock:
               cursor = self._conn.execute(sql, params)
               self._conn.commit()
               return cursor

       def _execute_read(self, sql, params=()):
           return self._conn.execute(sql, params)
       ```
    4. 重构 _get_conn() 返回 self._conn（兼容已有调用方如 routes.py 中的 _db._get_conn()）
    5. 重构 _init_tables() 使用 self._conn 直接执行
    6. 重构已有方法：topic_exists 使用 _execute_read，insert_topic/insert_article/mark_published 使用 _execute_write
    7. 重构 get_unpublished_articles/get_recent_articles 使用 _execute_read
  </action>
  <verify>
    <automated>cd /Users/wencai/github/newsAgent && python -m pytest tests/test_smoke.py -v</automated>
  </verify>
  <done>
    - database.py 包含 import threading
    - __init__ 中 self._conn 是 sqlite3.connect 返回值且 check_same_thread=False
    - __init__ 中执行 PRAGMA journal_mode=WAL
    - __init__ 中创建 self._write_lock = threading.Lock()
    - 所有写操作通过 _execute_write（含 with self._write_lock）
    - 所有读操作通过 _execute_read
    - tests/test_smoke.py 全部通过（向后兼容验证）
  </done>
</task>

<task type="auto">
  <name>Task 2: 补齐 Database 缺失的 5 个方法</name>
  <read_first>
    /Users/wencai/github/newsAgent/src/storage/database.py
    /Users/wencai/github/newsAgent/src/web/routes.py
  </read_first>
  <files>src/storage/database.py</files>
  <action>
    在 Database 类中添加以下 5 个方法（在 mark_published 方法之后）：

    def get_dashboard_stats(self) -> dict:
      返回 dict，包含 topic_count, article_count, draft_count, published_count, recent_articles（list[dict]，最近 10 篇）
      使用 _execute_read 获取各 COUNT 和最近文章列表

    def get_topics(self, limit: int = 50) -> list[dict]:
      查询 hot_topics 表，ORDER BY id DESC LIMIT ?，返回 list[dict]

    def get_articles(self, status: str = None) -> list[dict]:
      如果 status 为 "draft" 或 "published"，WHERE status=? ORDER BY generated_at DESC
      否则 SELECT * FROM articles ORDER BY generated_at DESC
      返回 list[dict]

    def get_article(self, article_id: int) -> dict | None:
      SELECT * FROM articles WHERE id=?，返回 dict 或 None

    def delete_article(self, article_id: int) -> bool:
      使用 _execute_write 执行 DELETE FROM articles WHERE id=?
      返回 cursor.rowcount > 0

    所有方法使用 _execute_read 或 _execute_write，不直接调用 self._conn.execute
  </action>
  <verify>
    <automated>cd /Users/wencai/github/newsAgent && python -c "
from src.storage.database import Database
db = Database(':memory:')
assert hasattr(db, 'get_dashboard_stats')
assert hasattr(db, 'get_topics')
assert hasattr(db, 'get_articles')
assert hasattr(db, 'get_article')
assert hasattr(db, 'delete_article')
stats = db.get_dashboard_stats()
assert isinstance(stats, dict) and 'topic_count' in stats
topics = db.get_topics()
assert isinstance(topics, list)
articles = db.get_articles()
assert isinstance(articles, list)
print('All methods verified OK')
db._conn.close()
"</automated>
  </verify>
  <done>
    - database.py 包含 get_dashboard_stats/get_topics/get_articles/get_article/delete_article
    - get_dashboard_stats 返回 dict 包含 topic_count/article_count/draft_count/published_count/recent_articles
    - get_topics 返回 list[dict]，支持 limit 参数
    - get_articles 返回 list[dict]，支持 status 筛选参数
    - get_article 返回 dict 或 None
    - delete_article 返回 bool
    - 所有方法使用 _execute_read/_execute_write
  </done>
</task>

<task type="auto">
  <name>Task 3: 编写 Database 单元测试和并发测试</name>
  <read_first>
    /Users/wencai/github/newsAgent/src/storage/database.py
    /Users/wencai/github/newsAgent/tests/conftest.py
  </read_first>
  <files>tests/test_database.py, tests/conftest.py</files>
  <action>
    1. 更新 tests/conftest.py，将 db fixture 改为使用 :memory:（单连接重构后兼容）：
       ```python
       @pytest.fixture
       def db():
           database = Database(':memory:')
           yield database
           database._conn.close()
       ```

    2. 创建 tests/test_database.py，包含以下测试类和方法：

       TestDatabaseInit:
         - test_single_connection: 验证 _get_conn() 返回同一 self._conn
         - test_wal_mode: 验证 PRAGMA journal_mode 返回 "wal"
         - test_write_lock_exists: 验证 _write_lock 是 threading.Lock 实例

       TestDashboardStats:
         - test_empty_dashboard: 空库返回全 0 和空列表
         - test_dashboard_with_data: 插入 1 热点 2 文章，验证计数正确

       TestGetTopics:
         - test_get_topics_empty: 空库返回空列表
         - test_get_topics_with_limit: 插入 5 条取 3 条，验证 limit 生效

       TestGetArticles:
         - test_get_articles_all: 插入 2 篇，验证返回 2 篇
         - test_get_articles_by_status: 1 draft + 1 published，验证按状态筛选正确

       TestGetArticle:
         - test_get_existing: 验证返回正确文章 dict
         - test_get_nonexistent: 验证返回 None

       TestDeleteArticle:
         - test_delete_existing: 删除后返回 True，再查返回 None
         - test_delete_nonexistent: 返回 False

       TestConcurrentWrites:
         - test_concurrent_writes: 20 个线程并发 insert_topic，验证无 SQLITE_BUSY 错误
  </action>
  <verify>
    <automated>cd /Users/wencai/github/newsAgent && python -m pytest tests/test_database.py -v</automated>
  </verify>
  <done>
    - tests/conftest.py 使用 Database(':memory:')
    - tests/test_database.py 存在且包含 8 个测试类
    - test_concurrent_writes 验证 20 个线程并发写入无 SQLITE_BUSY 错误
    - test_wal_mode 验证 WAL 模式已启用
    - test_single_connection 验证 _get_conn 返回同一连接
    - 全部测试通过
  </done>
</task>

</tasks>

<verification>
```bash
# 运行全部测试
cd /Users/wencai/github/newsAgent && python -m pytest tests/ -v

# 预期：test_smoke.py (3) + test_database.py (>12) 全部通过
```

```bash
# 验证 WAL 模式
cd /Users/wencai/github/newsAgent && python -c "
from src.storage.database import Database
db = Database(':memory:')
mode = db._conn.execute('PRAGMA journal_mode').fetchone()[0]
print(f'Journal mode: {mode}')
assert mode == 'wal'
db._conn.close()
"
```

```bash
# 验证路由层可零 SQL（grep 检查 database.py 中的 _execute_write/_execute_read）
grep -c "_execute_write\|_execute_read" /Users/wencai/github/newsAgent/src/storage/database.py
# 预期：>= 8（所有方法都使用了辅助方法）
```
</verification>

<success_criteria>
- Database 类使用单连接（self._conn），check_same_thread=False
- WAL 模式已启用（PRAGMA journal_mode=WAL）
- threading.Lock 保护所有写操作（_execute_write 方法中 with self._write_lock）
- 5 个新方法全部实现：get_dashboard_stats, get_topics, get_articles, get_article, delete_article
- 已有方法保持向后兼容（tests/test_smoke.py 仍通过）
- tests/test_database.py 全部通过，包含并发写入测试
- :memory: 数据库 fixture 在 conftest.py 中正常工作
</success_criteria>

<output>
完成后创建 `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
